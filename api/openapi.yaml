openapi: 3.0.3
info:
  title: QLNS API
  version: 1.0.0
  description: >-
    Contract-first API for the three modules selected for first delivery —
    Core HR (including Contracts), Recruitment and Attendance & Leave —
    plus Reports and Administration.
    Workflow changes use explicit commands, ETag/If-Match concurrency control,
    server-side authorization, audit logging and transactional outbox delivery.
servers:
  - url: http://localhost:5000
    description: Local ASP.NET Core development server
security:
  - bearerAuth: []
tags:
  - { name: Recruitment Requisitions }
  - { name: Recruitment Intake }
  - { name: Recruitment Pipeline }
  - { name: Recruitment Interviews }
  - { name: Recruitment Evaluations }
  - { name: Recruitment Offers }
  - { name: Employees }
  - { name: Organization }
  - { name: Onboarding }
  - { name: Employee Events }
  - { name: Employee Documents }
  - { name: Contracts }
  - { name: Contract Addenda }
  - { name: Reports }
  - { name: Report Exports }
  - { name: Attendance Shifts }
  - { name: Attendance Holidays }
  - { name: Attendance Events }
  - { name: Attendance Overtime }
  - { name: Timesheets }
  - { name: Timesheet Periods }
  - { name: Attendance Corrections }
  - { name: Leave }
  - { name: Leave Policy }
  - { name: Probation }
  - { name: Offboarding }
  - { name: Administration Users }
  - { name: Administration Authorization }
  - { name: Administration Audit }
  - { name: Administration Deliveries }
  - { name: Administration Integrations }
  - { name: Operations }

paths:
  /api/v1/recruitment/requisitions:
    get:
      tags: [Recruitment Requisitions]
      operationId: listRecruitmentRequisitions
      summary: Search requisitions in the actor's data scope
      x-requirement: REC-01.1, REC-01.2
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/Search'
        - { name: departmentId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: status, in: query, schema: { $ref: '#/components/schemas/RequisitionStatus' } }
        - name: sort
          in: query
          description: Allowlist is createdAt, -createdAt, closingDate, -closingDate.
          schema: { type: string, default: -createdAt }
      responses:
        '200': { description: Paged requisitions, content: { application/json: { schema: { $ref: '#/components/schemas/RequisitionPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [Recruitment Requisitions]
      operationId: createRecruitmentRequisition
      summary: Create a draft requisition
      x-requirement: REC-01.1
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/RequisitionWrite' } } } }
      responses:
        '201':
          description: Draft created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Requisition' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/recruitment/requisitions/{requisitionId}:
    parameters: [{ $ref: '#/components/parameters/RequisitionId' }]
    get:
      tags: [Recruitment Requisitions]
      operationId: getRecruitmentRequisition
      summary: Get one requisition
      x-requirement: REC-01.1, REC-01.2
      responses:
        '200':
          description: Requisition
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Requisition' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    put:
      tags: [Recruitment Requisitions]
      operationId: replaceRecruitmentRequisitionDraft
      summary: Replace editable fields of a draft or returned requisition
      x-requirement: REC-01.1
      parameters: [{ $ref: '#/components/parameters/IfMatch' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/RequisitionWrite' } } } }
      responses:
        '200':
          description: Draft updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Requisition' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/recruitment/requisitions/{requisitionId}/{action}:
    post:
      tags: [Recruitment Requisitions]
      operationId: transitionRecruitmentRequisition
      summary: Submit, approve, reject, publish, close or cancel a requisition
      description: Reject requires a reason; publish requires at least one channel.
      x-requirement: REC-01.2
      parameters:
        - $ref: '#/components/parameters/RequisitionId'
        - { name: action, in: path, required: true, schema: { type: string, enum: [submit, approve, reject, publish, close, cancel] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/RequisitionAction' } } } }
      responses:
        '200':
          description: Transition completed
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Requisition' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/recruitment/resumes:
    post:
      tags: [Recruitment Intake]
      operationId: startRecruitmentIntake
      summary: Scan and parse a PDF, DOC or DOCX resume up to 10 MiB
      x-requirement: REC-02.1
      requestBody: { required: true, content: { multipart/form-data: { schema: { $ref: '#/components/schemas/ResumeUpload' } } } }
      responses:
        '202':
          description: Intake accepted
          headers: { Location: { $ref: '#/components/headers/Location' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/CandidateIntake' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '413': { $ref: '#/components/responses/PayloadTooLarge' }
        '415': { $ref: '#/components/responses/UnsupportedMediaType' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/recruitment/intakes/{intakeId}:
    get:
      tags: [Recruitment Intake]
      operationId: getRecruitmentIntake
      summary: Get scan, parse and duplicate-detection state
      x-requirement: REC-02.1, REC-02.2
      parameters: [{ $ref: '#/components/parameters/IntakeId' }]
      responses:
        '200': { description: Intake, content: { application/json: { schema: { $ref: '#/components/schemas/CandidateIntake' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/recruitment/intakes/{intakeId}/confirm:
    post:
      tags: [Recruitment Intake]
      operationId: confirmRecruitmentIntake
      summary: Confirm parsed data, resolve duplicates and create an application
      x-requirement: REC-02.2
      parameters:
        - $ref: '#/components/parameters/IntakeId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ConfirmIntake' } } } }
      responses:
        '201':
          description: Application created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/RecruitmentApplicationDetail' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/recruitment/pipeline:
    get:
      tags: [Recruitment Pipeline]
      operationId: getRecruitmentPipeline
      summary: Get a filtered, paged Kanban pipeline
      x-requirement: REC-03.1
      parameters:
        - { name: requisitionId, in: query, required: true, schema: { type: integer, format: int64, minimum: 1 } }
        - $ref: '#/components/parameters/Search'
        - { name: stage, in: query, schema: { $ref: '#/components/schemas/RecruitmentStage' } }
        - { name: minimumAiScore, in: query, schema: { type: number, minimum: 0, maximum: 100 } }
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
      responses:
        '200': { description: Pipeline, content: { application/json: { schema: { $ref: '#/components/schemas/RecruitmentPipeline' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/recruitment/applications/{applicationId}:
    get:
      tags: [Recruitment Pipeline]
      operationId: getRecruitmentApplication
      summary: Get one authorized recruitment application
      x-requirement: REC-03.1, REC-03.2
      parameters: [{ $ref: '#/components/parameters/ApplicationId' }]
      responses:
        '200':
          description: Application
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/RecruitmentApplication' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/recruitment/applications/{applicationId}/advance:
    post:
      tags: [Recruitment Pipeline]
      operationId: advanceRecruitmentApplication
      summary: Advance an application exactly one active stage
      x-requirement: REC-03.2
      parameters: [{ $ref: '#/components/parameters/ApplicationId' }, { $ref: '#/components/parameters/IfMatch' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/AdvanceApplicationRequest' } } } }
      responses:
        '200':
          description: Application advanced
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/RecruitmentApplication' } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/recruitment/applications/{applicationId}/{terminalAction}:
    post:
      tags: [Recruitment Pipeline]
      operationId: terminateRecruitmentApplication
      summary: Reject or withdraw an application with a reason
      x-requirement: REC-03.2
      parameters:
        - $ref: '#/components/parameters/ApplicationId'
        - { name: terminalAction, in: path, required: true, schema: { type: string, enum: [reject, withdraw] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ReasonRequest' } } } }
      responses:
        '200':
          description: Application terminated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/RecruitmentApplication' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/recruitment/interviews:
    get:
      tags: [Recruitment Interviews]
      operationId: listRecruitmentInterviews
      summary: Search interviews visible to the actor
      x-requirement: REC-04.1
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - { name: applicationId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: interviewerUserId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: from, in: query, schema: { type: string, format: date-time } }
        - { name: to, in: query, schema: { type: string, format: date-time } }
        - { name: status, in: query, schema: { $ref: '#/components/schemas/InterviewStatus' } }
      responses:
        '200': { description: Paged interviews, content: { application/json: { schema: { $ref: '#/components/schemas/InterviewPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [Recruitment Interviews]
      operationId: scheduleRecruitmentInterview
      summary: Schedule an interview and enqueue invitations
      x-requirement: REC-04.1
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/InterviewWrite' } } } }
      responses:
        '201':
          description: Interview scheduled
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Interview' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/recruitment/interviews/{interviewId}/{action}:
    post:
      tags: [Recruitment Interviews]
      operationId: transitionRecruitmentInterview
      summary: Reschedule, complete or cancel an interview
      x-requirement: REC-04.1
      parameters:
        - $ref: '#/components/parameters/InterviewId'
        - { name: action, in: path, required: true, schema: { type: string, enum: [reschedule, complete, cancel] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/InterviewAction' } } } }
      responses:
        '200':
          description: Interview updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Interview' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/recruitment/interviews/{interviewId}/evaluations:
    get:
      tags: [Recruitment Evaluations]
      operationId: listInterviewEvaluations
      summary: List evaluations subject to blind-evaluation policy
      x-requirement: REC-05.1
      parameters: [{ $ref: '#/components/parameters/InterviewId' }]
      responses:
        '200': { description: Evaluations, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/Evaluation' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    post:
      tags: [Recruitment Evaluations]
      operationId: submitInterviewEvaluation
      summary: Submit an immutable scorecard
      x-requirement: REC-05.1
      parameters: [{ $ref: '#/components/parameters/InterviewId' }, { $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/EvaluationWrite' } } } }
      responses:
        '201':
          description: Evaluation submitted
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Evaluation' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/recruitment/evaluations/{evaluationId}/unlock:
    post:
      tags: [Recruitment Evaluations]
      operationId: unlockInterviewEvaluation
      summary: Unlock by creating a new auditable evaluation version
      x-requirement: REC-05.1
      parameters: [{ $ref: '#/components/parameters/EvaluationId' }, { $ref: '#/components/parameters/IfMatch' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ReasonRequest' } } } }
      responses:
        '200':
          description: New evaluation version
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Evaluation' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }

  /api/v1/recruitment/offers:
    get:
      tags: [Recruitment Offers]
      operationId: listRecruitmentOffers
      summary: Search offers in the actor's scope
      x-requirement: REC-06.1
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - { name: applicationId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: status, in: query, schema: { $ref: '#/components/schemas/OfferStatus' } }
      responses:
        '200': { description: Paged offers, content: { application/json: { schema: { $ref: '#/components/schemas/OfferPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [Recruitment Offers]
      operationId: createRecruitmentOffer
      summary: Create a draft offer
      x-requirement: REC-06.1
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/OfferWrite' } } } }
      responses:
        '201':
          description: Draft offer created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Offer' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/recruitment/offers/{offerId}:
    get:
      tags: [Recruitment Offers]
      operationId: getRecruitmentOffer
      summary: Get one offer
      x-requirement: REC-06.1
      parameters: [{ $ref: '#/components/parameters/OfferId' }]
      responses:
        '200':
          description: Offer
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Offer' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/recruitment/offers/{offerId}/{action}:
    post:
      tags: [Recruitment Offers]
      operationId: transitionRecruitmentOffer
      summary: Approve, send, extend or cancel an offer
      x-requirement: REC-06.1
      parameters:
        - $ref: '#/components/parameters/OfferId'
        - { name: action, in: path, required: true, schema: { type: string, enum: [approve, send, extend, cancel] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/OfferAction' } } } }
      responses:
        '200':
          description: Offer updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Offer' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/recruitment/offers/{offerId}/response:
    post:
      tags: [Recruitment Offers]
      operationId: respondToRecruitmentOffer
      summary: Accept or decline as the candidate
      description: Acceptance idempotently creates an employee, initial contract and onboarding checklist.
      x-requirement: REC-06.2
      security: [{ offerToken: [] }]
      parameters: [{ $ref: '#/components/parameters/OfferId' }, { $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/OfferResponse' } } } }
      responses:
        '200': { description: Response recorded or replayed, content: { application/json: { schema: { $ref: '#/components/schemas/OfferResponseResult' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/employees:
    get:
      tags: [Employees]
      operationId: listEmployees
      summary: Search the scope-filtered employee directory
      x-requirement: EMP-01.1
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/Search'
        - { name: departmentId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: positionId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: status, in: query, schema: { $ref: '#/components/schemas/EmployeeStatus' } }
        - { name: sort, in: query, description: Allowlist is name, -name, employeeCode, hireDate., schema: { type: string, default: name } }
      responses:
        '200': { description: Paged employees, content: { application/json: { schema: { $ref: '#/components/schemas/EmployeePage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/employees/{employeeId}:
    get:
      tags: [Employees]
      operationId: getEmployee
      summary: Get an employee with sensitive fields masked by policy
      x-requirement: EMP-01.1
      parameters: [{ $ref: '#/components/parameters/EmployeeId' }]
      responses:
        '200':
          description: Employee
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/EmployeeDetail' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/employees/{employeeId}/profile:
    patch:
      tags: [Employees]
      operationId: updateEmployeePersonalProfile
      summary: Update only personal fields allowed by field policy
      description: Work email, department, position, manager, status and salary cannot be changed here.
      x-requirement: EMP-01.2
      parameters: [{ $ref: '#/components/parameters/EmployeeId' }, { $ref: '#/components/parameters/IfMatch' }]
      requestBody: { required: true, content: { application/merge-patch+json: { schema: { $ref: '#/components/schemas/PersonalProfilePatch' } } } }
      responses:
        '200':
          description: Profile updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/EmployeeDetail' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/organization/chart:
    get:
      tags: [Organization]
      operationId: getOrganizationChart
      summary: Get hierarchical departments with managers and headcount
      x-requirement: EMP-02.1
      parameters:
        - { name: rootDepartmentId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: depth, in: query, schema: { type: integer, minimum: 1, maximum: 10, default: 10 } }
      responses:
        '200': { description: Organization tree, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/OrganizationNode' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/organization/departments:
    get:
      tags: [Organization]
      operationId: listDepartments
      summary: List departments
      x-requirement: EMP-02.1
      responses:
        '200': { description: Departments, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/Department' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
    post:
      tags: [Organization]
      operationId: createDepartment
      summary: Create a department
      x-requirement: EMP-02.1
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/DepartmentWrite' } } } }
      responses:
        '201':
          description: Department created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Department' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/organization/departments/{departmentId}:
    parameters: [{ $ref: '#/components/parameters/DepartmentId' }]
    put:
      tags: [Organization]
      operationId: replaceDepartment
      summary: Replace department metadata
      x-requirement: EMP-02.1
      parameters: [{ $ref: '#/components/parameters/IfMatch' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/DepartmentWrite' } } } }
      responses:
        '200':
          description: Department updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Department' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
    delete:
      tags: [Organization]
      operationId: deleteDepartment
      summary: Delete a department only when it has no child, employee or open requisition
      x-requirement: EMP-02.1
      parameters: [{ $ref: '#/components/parameters/IfMatch' }]
      responses:
        '204': { description: Department deleted }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }

  /api/v1/organization/positions:
    get:
      tags: [Organization]
      operationId: listPositions
      summary: List position definitions
      x-requirement: EMP-02.1
      responses:
        '200': { description: Positions, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/Position' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
    post:
      tags: [Organization]
      operationId: createPosition
      summary: Create a position definition
      x-requirement: EMP-02.1
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/PositionWrite' } } } }
      responses:
        '201':
          description: Position created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Position' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/organization/positions/{positionId}:
    put:
      tags: [Organization]
      operationId: replacePosition
      summary: Replace position metadata
      x-requirement: EMP-02.1
      parameters: [{ $ref: '#/components/parameters/PositionId' }, { $ref: '#/components/parameters/IfMatch' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/PositionWrite' } } } }
      responses:
        '200':
          description: Position updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Position' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
  /api/v1/onboarding/tasks:
    get:
      tags: [Onboarding]
      operationId: listOnboardingTasks
      summary: Search onboarding tasks and overdue work
      x-requirement: EMP-03.1
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - { name: employeeId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: assignedToUserId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: status, in: query, schema: { $ref: '#/components/schemas/OnboardingTaskStatus' } }
        - { name: overdue, in: query, schema: { type: boolean } }
      responses:
        '200': { description: Paged tasks, content: { application/json: { schema: { $ref: '#/components/schemas/OnboardingTaskPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/onboarding/tasks/{taskId}/{action}:
    post:
      tags: [Onboarding]
      operationId: transitionOnboardingTask
      summary: Start, complete or reopen an onboarding task
      x-requirement: EMP-03.1
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - { name: action, in: path, required: true, schema: { type: string, enum: [start, complete, reopen] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/ReasonRequest' } } } }
      responses:
        '200':
          description: Task updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/OnboardingTask' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }

  /api/v1/onboarding/tasks/{taskId}:
    put:
      tags: [Onboarding]
      operationId: updateOnboardingTaskAssignment
      summary: Update task ownership, description and due date
      x-requirement: EMP-03.1
      parameters: [{ $ref: '#/components/parameters/TaskId' }, { $ref: '#/components/parameters/IfMatch' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/OnboardingTaskWrite' } } } }
      responses:
        '200':
          description: Task updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/OnboardingTask' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/employees/{employeeId}/events:
    get:
      tags: [Employee Events]
      operationId: listEmployeeEvents
      summary: List immutable movement history
      x-requirement: EMP-04.1
      parameters: [{ $ref: '#/components/parameters/EmployeeId' }, { $ref: '#/components/parameters/Page' }, { $ref: '#/components/parameters/PageSize' }]
      responses:
        '200': { description: Paged events, content: { application/json: { schema: { $ref: '#/components/schemas/EmployeeEventPage' } } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    post:
      tags: [Employee Events]
      operationId: createEmployeeEvent
      summary: Create a draft employee movement proposal
      x-requirement: EMP-04.1
      parameters: [{ $ref: '#/components/parameters/EmployeeId' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/EmployeeEventWrite' } } } }
      responses:
        '201':
          description: Draft event created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/EmployeeEvent' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/employee-events/{eventId}/{action}:
    post:
      tags: [Employee Events]
      operationId: transitionEmployeeEvent
      summary: Submit, approve or cancel an event; applied events are immutable
      x-requirement: EMP-04.1
      parameters:
        - $ref: '#/components/parameters/EventId'
        - { name: action, in: path, required: true, schema: { type: string, enum: [submit, approve, cancel] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/ReasonRequest' } } } }
      responses:
        '200':
          description: Event updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/EmployeeEvent' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }

  /api/v1/employees/{employeeId}/documents:
    get:
      tags: [Employee Documents]
      operationId: listEmployeeDocuments
      summary: List authorized private-document metadata
      x-requirement: EMP-05.1
      parameters: [{ $ref: '#/components/parameters/EmployeeId' }]
      responses:
        '200': { description: Documents, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/EmployeeDocument' } } } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    post:
      tags: [Employee Documents]
      operationId: uploadEmployeeDocument
      summary: Scan and store a new private document version
      x-requirement: EMP-05.1
      parameters: [{ $ref: '#/components/parameters/EmployeeId' }]
      requestBody: { required: true, content: { multipart/form-data: { schema: { $ref: '#/components/schemas/EmployeeDocumentUpload' } } } }
      responses:
        '201':
          description: Document stored
          headers: { Location: { $ref: '#/components/headers/Location' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/EmployeeDocument' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '413': { $ref: '#/components/responses/PayloadTooLarge' }
        '415': { $ref: '#/components/responses/UnsupportedMediaType' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/employee-documents/{documentId}/download-url:
    post:
      tags: [Employee Documents]
      operationId: createEmployeeDocumentDownloadUrl
      summary: Create an authorized download URL valid for at most 15 minutes
      x-requirement: EMP-05.1
      parameters: [{ $ref: '#/components/parameters/DocumentId' }]
      responses:
        '200': { description: Signed URL, content: { application/json: { schema: { $ref: '#/components/schemas/SignedDownload' } } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/contracts:
    get:
      tags: [Contracts]
      operationId: listContracts
      summary: Search contracts; employees can only access their own
      x-requirement: CON-01.1, CON-01.2
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - { name: employeeId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: type, in: query, schema: { $ref: '#/components/schemas/ContractType' } }
        - { name: status, in: query, schema: { $ref: '#/components/schemas/ContractStatus' } }
      responses:
        '200': { description: Paged contracts, content: { application/json: { schema: { $ref: '#/components/schemas/ContractPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [Contracts]
      operationId: createContract
      summary: Create a draft employment contract
      x-requirement: CON-01.1
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ContractWrite' } } } }
      responses:
        '201':
          description: Draft created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Contract' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/contracts/expiring:
    get:
      tags: [Contracts]
      operationId: listExpiringContracts
      summary: List contracts at configured expiry-alert thresholds
      x-requirement: CON-02.1
      parameters:
        - { name: asOf, in: query, schema: { type: string, format: date } }
        - { name: withinDays, in: query, schema: { type: integer, minimum: 1, maximum: 365, default: 45 } }
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
      responses:
        '200': { description: Expiring contracts, content: { application/json: { schema: { $ref: '#/components/schemas/ExpiringContractPage' } } } }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/contracts/{contractId}:
    parameters: [{ $ref: '#/components/parameters/ContractId' }]
    get:
      tags: [Contracts]
      operationId: getContract
      summary: Get one authorized contract
      x-requirement: CON-01.1, CON-01.2
      responses:
        '200':
          description: Contract
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Contract' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    put:
      tags: [Contracts]
      operationId: replaceContractDraft
      summary: Replace editable fields of a draft contract
      x-requirement: CON-01.1
      parameters: [{ $ref: '#/components/parameters/IfMatch' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ContractWrite' } } } }
      responses:
        '200':
          description: Draft updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Contract' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/contracts/{contractId}/{action}:
    post:
      tags: [Contracts]
      operationId: transitionContract
      summary: Approve, activate, terminate or cancel a contract
      x-requirement: CON-01.1, CON-01.2
      parameters:
        - $ref: '#/components/parameters/ContractId'
        - { name: action, in: path, required: true, schema: { type: string, enum: [approve, activate, terminate, cancel] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/ContractAction' } } } }
      responses:
        '200':
          description: Contract updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Contract' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }

  /api/v1/contracts/{contractId}/signed-document:
    post:
      tags: [Contracts]
      operationId: uploadSignedContractDocument
      summary: Upload a signed PDF before activation
      x-requirement: CON-01.2
      parameters: [{ $ref: '#/components/parameters/ContractId' }, { $ref: '#/components/parameters/IfMatch' }]
      requestBody:
        required: true
        content: { multipart/form-data: { schema: { type: object, additionalProperties: false, required: [file], properties: { file: { type: string, format: binary } } } } }
      responses:
        '200':
          description: Document stored
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Contract' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '415': { $ref: '#/components/responses/UnsupportedMediaType' }

  /api/v1/contracts/{contractId}/download-url:
    post:
      tags: [Contracts]
      operationId: createContractDownloadUrl
      summary: Create a short-lived URL for an authorized signed-contract download
      x-requirement: CON-01.2
      parameters: [{ $ref: '#/components/parameters/ContractId' }]
      responses:
        '200': { description: Signed URL, content: { application/json: { schema: { $ref: '#/components/schemas/SignedDownload' } } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/contracts/{contractId}/addenda:
    get:
      tags: [Contract Addenda]
      operationId: listContractAddenda
      summary: List addenda without mutating the original contract
      x-requirement: CON-03.1
      parameters: [{ $ref: '#/components/parameters/ContractId' }]
      responses:
        '200': { description: Addenda, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/ContractAddendum' } } } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    post:
      tags: [Contract Addenda]
      operationId: createContractAddendum
      summary: Create a draft addendum with before and after terms
      x-requirement: CON-03.1
      parameters: [{ $ref: '#/components/parameters/ContractId' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ContractAddendumWrite' } } } }
      responses:
        '201':
          description: Draft addendum created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/ContractAddendum' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/contract-addenda/{addendumId}/{action}:
    post:
      tags: [Contract Addenda]
      operationId: transitionContractAddendum
      summary: Submit, approve, mark signed, make effective or cancel an addendum
      description: Making an addendum effective creates an employee event when master data changes.
      x-requirement: CON-03.1
      parameters:
        - $ref: '#/components/parameters/AddendumId'
        - { name: action, in: path, required: true, schema: { type: string, enum: [submit, approve, mark-signed, make-effective, cancel] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/ReasonRequest' } } } }
      responses:
        '200':
          description: Addendum updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/ContractAddendum' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }

  /api/v1/contract-addenda/{addendumId}/signed-document:
    post:
      tags: [Contract Addenda]
      operationId: uploadSignedContractAddendum
      summary: Upload the signed PDF before making an addendum effective
      x-requirement: CON-03.1
      parameters: [{ $ref: '#/components/parameters/AddendumId' }, { $ref: '#/components/parameters/IfMatch' }]
      requestBody:
        required: true
        content: { multipart/form-data: { schema: { type: object, additionalProperties: false, required: [file], properties: { file: { type: string, format: binary } } } } }
      responses:
        '200':
          description: Signed document stored
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/ContractAddendum' } } }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '415': { $ref: '#/components/responses/UnsupportedMediaType' }

  /api/v1/reports/headcount:
    get:
      tags: [Reports]
      operationId: getHeadcountReport
      summary: Get authorized headcount and workforce KPIs
      description: Every metric includes its formula identifier and the read-model refresh time.
      x-requirement: REP-01.1
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/ReportFrom'
        - $ref: '#/components/parameters/ReportTo'
        - { name: departmentId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: positionId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: location, in: query, schema: { type: string, maxLength: 255 } }
      responses:
        '200': { description: Headcount report, content: { application/json: { schema: { $ref: '#/components/schemas/HeadcountReport' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/reports/recruitment:
    get:
      tags: [Reports]
      operationId: getRecruitmentReport
      summary: Get recruitment funnel, velocity, source and offer KPIs
      x-requirement: REP-02.1
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/ReportFrom'
        - $ref: '#/components/parameters/ReportTo'
        - { name: departmentId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: positionId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: location, in: query, schema: { type: string, maxLength: 255 } }
        - { name: requisitionId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
      responses:
        '200': { description: Recruitment report, content: { application/json: { schema: { $ref: '#/components/schemas/RecruitmentReport' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/reports/exports:
    post:
      tags: [Report Exports]
      operationId: createReportExport
      summary: Create an audited asynchronous report export
      description: Exports are data-scope filtered, field-allowlisted and watermarked by the server.
      x-requirement: REP-01.1, REP-02.1, SYS-04
      x-implementation-status: proposed
      parameters: [{ $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CreateReportExportRequest' } } } }
      responses:
        '202':
          description: Export job accepted
          headers: { Location: { $ref: '#/components/headers/Location' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/ReportExport' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/reports/exports/{exportId}:
    get:
      tags: [Report Exports]
      operationId: getReportExport
      summary: Get report export status
      x-requirement: SYS-04
      x-implementation-status: proposed
      parameters: [{ $ref: '#/components/parameters/ExportId' }]
      responses:
        '200': { description: Export status, content: { application/json: { schema: { $ref: '#/components/schemas/ReportExport' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/reports/exports/{exportId}/download-url:
    post:
      tags: [Report Exports]
      operationId: createReportExportDownloadUrl
      summary: Create a short-lived authorized URL for a completed export
      x-requirement: SYS-04
      x-implementation-status: proposed
      parameters: [{ $ref: '#/components/parameters/ExportId' }]
      responses:
        '200': { description: Signed download, content: { application/json: { schema: { $ref: '#/components/schemas/SignedDownload' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }

  /api/v1/attendance/shifts:
    get:
      tags: [Attendance Shifts]
      operationId: listWorkShifts
      summary: List work-shift definitions
      x-requirement: ATT-01
      x-implementation-status: discovery-required
      parameters:
        - { name: active, in: query, schema: { type: boolean } }
      responses:
        '200': { description: Shift definitions, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/WorkShift' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [Attendance Shifts]
      operationId: createWorkShift
      summary: Create a work-shift definition
      x-requirement: ATT-01
      x-implementation-status: discovery-required
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/WorkShiftWrite' } } } }
      responses:
        '201':
          description: Shift created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/WorkShift' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/attendance/shifts/{shiftId}:
    parameters: [{ $ref: '#/components/parameters/ShiftId' }]
    get:
      tags: [Attendance Shifts]
      operationId: getWorkShift
      summary: Get one work-shift definition
      x-requirement: ATT-01
      x-implementation-status: discovery-required
      responses:
        '200':
          description: Shift
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/WorkShift' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    put:
      tags: [Attendance Shifts]
      operationId: replaceWorkShift
      summary: Replace a work-shift definition
      x-requirement: ATT-01
      x-implementation-status: discovery-required
      parameters: [{ $ref: '#/components/parameters/IfMatch' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/WorkShiftWrite' } } } }
      responses:
        '200':
          description: Shift updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/WorkShift' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/attendance/schedules:
    get:
      tags: [Attendance Shifts]
      operationId: listWorkSchedules
      summary: Get employee shift assignments for a week
      x-requirement: ATT-01
      x-implementation-status: discovery-required
      parameters:
        - $ref: '#/components/parameters/WeekStart'
        - { name: employeeId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: departmentId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
      responses:
        '200': { description: Paged weekly schedules, content: { application/json: { schema: { $ref: '#/components/schemas/WorkSchedulePage' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    put:
      tags: [Attendance Shifts]
      operationId: replaceWorkScheduleAssignments
      summary: Replace selected weekly shift assignments atomically
      x-requirement: ATT-01
      x-implementation-status: discovery-required
      parameters:
        - $ref: '#/components/parameters/WeekStart'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ReplaceWorkScheduleRequest' } } } }
      responses:
        '200': { description: Updated assignments, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/WorkScheduleAssignment' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/attendance/events:
    post:
      tags: [Attendance Events]
      operationId: recordAttendanceEvent
      summary: Record an employee check-in or check-out
      x-requirement: ATT-02
      x-implementation-status: discovery-required
      parameters: [{ $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceEventWrite' } } } }
      responses:
        '201':
          description: Attendance event recorded or deduplicated
          headers: { Location: { $ref: '#/components/headers/Location' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceEvent' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/integrations/attendance/events:
    post:
      tags: [Attendance Events]
      operationId: ingestDeviceAttendanceEvent
      summary: Ingest a signed and idempotent attendance-device event
      x-requirement: ATT-02, SYS-04
      x-implementation-status: discovery-required
      security: [{ deviceAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/DeviceAttendanceEventWrite' } } } }
      responses:
        '202': { description: Device event accepted, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceEvent' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/attendance/timesheets:
    get:
      tags: [Timesheets]
      operationId: listTimesheets
      summary: Get calculated personal or team timesheets
      description: Calculated values expose the policy version used; clients must not recalculate payroll-grade totals.
      x-requirement: ATT-02
      x-implementation-status: discovery-required
      parameters:
        - { name: from, in: query, required: true, schema: { type: string, format: date } }
        - { name: to, in: query, required: true, schema: { type: string, format: date } }
        - { name: employeeId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: departmentId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: status, in: query, schema: { $ref: '#/components/schemas/TimesheetStatus' } }
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
      responses:
        '200': { description: Paged timesheet rows, content: { application/json: { schema: { $ref: '#/components/schemas/TimesheetPage' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/attendance/corrections:
    get:
      tags: [Attendance Corrections]
      operationId: listAttendanceCorrections
      summary: Search attendance correction requests
      x-requirement: ATT-02
      x-implementation-status: discovery-required
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - { name: employeeId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: status, in: query, schema: { $ref: '#/components/schemas/ApprovalStatus' } }
      responses:
        '200': { description: Paged corrections, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceCorrectionPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [Attendance Corrections]
      operationId: createAttendanceCorrection
      summary: Request a correction to one workday's attendance events
      x-requirement: ATT-02
      x-implementation-status: discovery-required
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceCorrectionWrite' } } } }
      responses:
        '201':
          description: Pending correction created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceCorrection' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/attendance/corrections/{correctionId}/{action}:
    post:
      tags: [Attendance Corrections]
      operationId: transitionAttendanceCorrection
      summary: Approve, reject or cancel an attendance correction
      x-requirement: ATT-02
      x-implementation-status: discovery-required
      parameters:
        - $ref: '#/components/parameters/CorrectionId'
        - { name: action, in: path, required: true, schema: { type: string, enum: [approve, reject, cancel] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/DecisionRequest' } } } }
      responses:
        '200':
          description: Correction updated and approved values recalculated when applicable
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/AttendanceCorrection' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }

  /api/v1/leave/types:
    get:
      tags: [Leave]
      operationId: listLeaveTypes
      summary: List leave types and their current policy metadata
      x-requirement: ATT-03
      x-implementation-status: discovery-required
      responses:
        '200': { description: Leave types, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/LeaveType' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
    post:
      tags: [Leave Policy]
      operationId: createLeaveType
      summary: Create a leave type in draft policy state
      description: A newly created leave type cannot be used to submit requests until its policy is approved.
      x-requirement: ATT-03.1
      x-implementation-status: discovery-required
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/LeaveTypeWrite' } } } }
      responses:
        '201':
          description: Draft leave type created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/LeaveType' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/leave/balances:
    get:
      tags: [Leave]
      operationId: listLeaveBalances
      summary: Get authorized leave balances for a calendar year
      x-requirement: ATT-03
      x-implementation-status: discovery-required
      parameters:
        - { name: year, in: query, required: true, schema: { type: integer, minimum: 2000, maximum: 2100 } }
        - { name: employeeId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
      responses:
        '200': { description: Leave balances, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/LeaveBalance' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/leave/requests:
    get:
      tags: [Leave]
      operationId: listLeaveRequests
      summary: Search personal, team or approval-queue leave requests
      x-requirement: ATT-03
      x-implementation-status: discovery-required
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - { name: employeeId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: status, in: query, schema: { $ref: '#/components/schemas/ApprovalStatus' } }
        - { name: from, in: query, schema: { type: string, format: date } }
        - { name: to, in: query, schema: { type: string, format: date } }
        - { name: pendingMyDecision, in: query, schema: { type: boolean } }
      responses:
        '200': { description: Paged leave requests, content: { application/json: { schema: { $ref: '#/components/schemas/LeaveRequestPage' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [Leave]
      operationId: createLeaveRequest
      summary: Submit a leave request and reserve balance atomically
      description: The server calculates requested units from schedule, holidays, timezone and policy version.
      x-requirement: ATT-03
      x-implementation-status: discovery-required
      parameters: [{ $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/LeaveRequestWrite' } } } }
      responses:
        '201':
          description: Pending request created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/LeaveRequest' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/leave/requests/{leaveRequestId}:
    get:
      tags: [Leave]
      operationId: getLeaveRequest
      summary: Get a leave request and its decision history
      x-requirement: ATT-03
      x-implementation-status: discovery-required
      parameters: [{ $ref: '#/components/parameters/LeaveRequestId' }]
      responses:
        '200':
          description: Leave request
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/LeaveRequest' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/leave/requests/{leaveRequestId}/{action}:
    post:
      tags: [Leave]
      operationId: transitionLeaveRequest
      summary: Approve, reject or cancel a leave request
      x-requirement: ATT-03
      x-implementation-status: discovery-required
      parameters:
        - $ref: '#/components/parameters/LeaveRequestId'
        - { name: action, in: path, required: true, schema: { type: string, enum: [approve, reject, cancel] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/DecisionRequest' } } } }
      responses:
        '200':
          description: Request and reserved balance updated atomically
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/LeaveRequest' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }

  /api/v1/leave/requests/batch-decision:
    post:
      tags: [Leave]
      operationId: decideLeaveRequestsBatch
      summary: Approve or reject multiple leave requests independently
      description: The response reports a result per item; one conflict does not hide other item results.
      x-requirement: ATT-03
      x-implementation-status: discovery-required
      parameters: [{ $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/BatchLeaveDecisionRequest' } } } }
      responses:
        '200': { description: Per-request decision results, content: { application/json: { schema: { $ref: '#/components/schemas/BatchLeaveDecisionResult' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/attendance/holidays:
    get:
      tags: [Attendance Holidays]
      operationId: listHolidays
      summary: List holidays for a calendar and year
      x-requirement: ATT-01.3
      x-implementation-status: discovery-required
      parameters:
        - { name: calendarCode, in: query, schema: { type: string, default: VN } }
        - { name: year, in: query, required: true, schema: { type: integer, minimum: 2000, maximum: 2100 } }
      responses:
        '200': { description: Holidays, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/Holiday' } } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
    post:
      tags: [Attendance Holidays]
      operationId: createHoliday
      summary: Add a holiday to a calendar
      x-requirement: ATT-01.3
      x-implementation-status: discovery-required
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/HolidayWrite' } } } }
      responses:
        '201':
          description: Holiday created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Holiday' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/attendance/holidays/{holidayId}:
    parameters: [{ $ref: '#/components/parameters/HolidayId' }]
    put:
      tags: [Attendance Holidays]
      operationId: replaceHoliday
      summary: Replace a holiday entry
      x-requirement: ATT-01.3
      x-implementation-status: discovery-required
      parameters: [{ $ref: '#/components/parameters/IfMatch' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/HolidayWrite' } } } }
      responses:
        '200':
          description: Holiday updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/Holiday' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }
    delete:
      tags: [Attendance Holidays]
      operationId: deleteHoliday
      summary: Remove a holiday entry that is not referenced by a computed timesheet
      x-requirement: ATT-01.3
      x-implementation-status: discovery-required
      parameters: [{ $ref: '#/components/parameters/IfMatch' }]
      responses:
        '204': { description: Holiday removed }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }

  /api/v1/attendance/overtime-requests:
    get:
      tags: [Attendance Overtime]
      operationId: listOvertimeRequests
      summary: Search overtime requests
      x-requirement: ATT-02.4
      x-implementation-status: discovery-required
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - { name: employeeId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: status, in: query, schema: { $ref: '#/components/schemas/ApprovalStatus' } }
        - { name: from, in: query, schema: { type: string, format: date } }
        - { name: to, in: query, schema: { type: string, format: date } }
        - { name: pendingMyDecision, in: query, schema: { type: boolean } }
      responses:
        '200': { description: Paged overtime requests, content: { application/json: { schema: { $ref: '#/components/schemas/OvertimeRequestPage' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [Attendance Overtime]
      operationId: createOvertimeRequest
      summary: Request overtime for one workday
      description: >-
        The server derives overtimeCategory and workCoefficient from the work schedule,
        holiday calendar and the active attendance policy. Client-supplied values are ignored.
      x-requirement: ATT-02.4
      x-implementation-status: discovery-required
      parameters: [{ $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/OvertimeRequestWrite' } } } }
      responses:
        '201':
          description: Pending overtime request created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/OvertimeRequest' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/attendance/overtime-requests/{overtimeRequestId}/{action}:
    post:
      tags: [Attendance Overtime]
      operationId: transitionOvertimeRequest
      summary: Approve, reject or cancel an overtime request
      description: Approving may grant fewer minutes than requested but never more.
      x-requirement: ATT-02.4
      x-implementation-status: discovery-required
      parameters:
        - $ref: '#/components/parameters/OvertimeRequestId'
        - { name: action, in: path, required: true, schema: { type: string, enum: [approve, reject, cancel] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/OvertimeDecisionRequest' } } } }
      responses:
        '200':
          description: Overtime request updated and timesheet minutes recalculated when applicable
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/OvertimeRequest' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/attendance/timesheet-periods:
    get:
      tags: [Timesheet Periods]
      operationId: listTimesheetPeriods
      summary: List timesheet periods
      x-requirement: ATT-04.2
      x-implementation-status: discovery-required
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - { name: status, in: query, schema: { $ref: '#/components/schemas/TimesheetPeriodStatus' } }
      responses:
        '200': { description: Paged timesheet periods, content: { application/json: { schema: { $ref: '#/components/schemas/TimesheetPeriodPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [Timesheet Periods]
      operationId: createTimesheetPeriod
      summary: Create a non-overlapping timesheet period
      x-requirement: ATT-04.2
      x-implementation-status: discovery-required
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/TimesheetPeriodWrite' } } } }
      responses:
        '201':
          description: Period created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/TimesheetPeriod' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/attendance/timesheet-periods/{periodId}/exceptions:
    get:
      tags: [Timesheet Periods]
      operationId: listTimesheetPeriodExceptions
      summary: List unresolved exceptions blocking approval of a period
      description: >-
        Exceptions include incomplete workdays, unexplained absences, pending corrections
        and approved overtime without matching attendance events.
      x-requirement: ATT-04.2
      x-implementation-status: discovery-required
      parameters:
        - $ref: '#/components/parameters/PeriodId'
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
      responses:
        '200': { description: Paged exceptions, content: { application/json: { schema: { $ref: '#/components/schemas/TimesheetExceptionPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/attendance/timesheet-periods/{periodId}/{action}:
    post:
      tags: [Timesheet Periods]
      operationId: transitionTimesheetPeriod
      summary: Submit, approve, lock or reopen a timesheet period
      description: >-
        Approval is refused while unresolved exceptions remain. Locking makes every record in
        the period immutable. Reopening requires a reason and is audited.
      x-requirement: ATT-04.2
      x-implementation-status: discovery-required
      parameters:
        - $ref: '#/components/parameters/PeriodId'
        - { name: action, in: path, required: true, schema: { type: string, enum: [submit, approve, lock, reopen] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/DecisionRequest' } } } }
      responses:
        '200':
          description: Period state updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/TimesheetPeriod' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/attendance/timesheet-periods/{periodId}/payroll-handoff:
    post:
      tags: [Timesheet Periods]
      operationId: handOffTimesheetPeriodToPayroll
      summary: Hand a locked period off to payroll
      description: Refused unless the period is locked. Records the handoff timestamp and reference.
      x-requirement: ATT-04.2
      x-implementation-status: discovery-required
      parameters:
        - $ref: '#/components/parameters/PeriodId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/PayrollHandoffRequest' } } } }
      responses:
        '200':
          description: Handoff recorded
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/TimesheetPeriod' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }

  /api/v1/leave/types/{leaveTypeId}:
    parameters: [{ $ref: '#/components/parameters/LeaveTypeId' }]
    put:
      tags: [Leave Policy]
      operationId: replaceLeaveType
      summary: Replace a draft leave-type policy
      description: Only a draft policy can be replaced. An approved policy requires a new policyVersion.
      x-requirement: ATT-03.1
      x-implementation-status: discovery-required
      parameters: [{ $ref: '#/components/parameters/IfMatch' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/LeaveTypeWrite' } } } }
      responses:
        '200':
          description: Leave type updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/LeaveType' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/leave/types/{leaveTypeId}/{action}:
    post:
      tags: [Leave Policy]
      operationId: transitionLeaveType
      summary: Approve, deactivate or reactivate a leave-type policy
      description: >-
        A leave type cannot be used to submit requests until its policy is approved.
        Approval records the approver and timestamp.
      x-requirement: ATT-03.1
      x-implementation-status: discovery-required
      parameters:
        - $ref: '#/components/parameters/LeaveTypeId'
        - { name: action, in: path, required: true, schema: { type: string, enum: [approve, deactivate, reactivate] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/DecisionRequest' } } } }
      responses:
        '200':
          description: Leave type policy updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/LeaveType' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }

  /api/v1/employees/{employeeId}/probation-review:
    parameters: [{ $ref: '#/components/parameters/EmployeeId' }]
    get:
      tags: [Probation]
      operationId: getProbationReview
      summary: Get the probation review for the employee's current probation contract
      x-requirement: EMP-06.1
      x-implementation-status: proposed
      responses:
        '200':
          description: Probation review
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/ProbationReview' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    put:
      tags: [Probation]
      operationId: submitProbationReview
      summary: Submit or update the probation assessment
      description: Only the assigned reviewer may submit. Recommending termination requires improvement notes.
      x-requirement: EMP-06.1
      x-implementation-status: proposed
      parameters: [{ $ref: '#/components/parameters/IfMatch' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ProbationReviewWrite' } } } }
      responses:
        '200':
          description: Assessment saved
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/ProbationReview' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/probation-reviews:
    get:
      tags: [Probation]
      operationId: listProbationReviews
      summary: Search probation reviews, including overdue ones
      x-requirement: EMP-06.1
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - { name: status, in: query, schema: { $ref: '#/components/schemas/ProbationReviewStatus' } }
        - { name: overdue, in: query, schema: { type: boolean } }
        - { name: departmentId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
      responses:
        '200': { description: Paged probation reviews, content: { application/json: { schema: { $ref: '#/components/schemas/ProbationReviewPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/probation-reviews/{reviewId}/{action}:
    post:
      tags: [Probation]
      operationId: decideProbationReview
      summary: Decide or cancel a probation review
      description: >-
        Deciding requires an outcome and an effective date, and atomically creates the matching
        employee event. A terminated outcome also opens an offboarding case.
      x-requirement: EMP-06.2
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/ReviewId'
        - { name: action, in: path, required: true, schema: { type: string, enum: [decide, cancel, unlock] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/ProbationDecisionRequest' } } } }
      responses:
        '200':
          description: Review decided and employee event created
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/ProbationReview' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/offboarding/cases:
    get:
      tags: [Offboarding]
      operationId: listOffboardingCases
      summary: Search offboarding cases
      x-requirement: EMP-07.1
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - { name: status, in: query, schema: { $ref: '#/components/schemas/OffboardingStatus' } }
        - { name: departmentId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
      responses:
        '200': { description: Paged offboarding cases, content: { application/json: { schema: { $ref: '#/components/schemas/OffboardingCasePage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [Offboarding]
      operationId: createOffboardingCase
      summary: Open an offboarding case
      description: >-
        Refused when the employee already has an open case. A shorter-than-required notice period
        is reported as a warning in the response rather than blocking the case.
      x-requirement: EMP-07.1
      x-implementation-status: proposed
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/OffboardingCaseWrite' } } } }
      responses:
        '201':
          description: Case created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/OffboardingCase' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/offboarding/cases/{caseId}:
    get:
      tags: [Offboarding]
      operationId: getOffboardingCase
      summary: Get one offboarding case with its blocking-task summary
      x-requirement: EMP-07.1
      x-implementation-status: proposed
      parameters: [{ $ref: '#/components/parameters/CaseId' }]
      responses:
        '200':
          description: Offboarding case
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/OffboardingCase' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/offboarding/cases/{caseId}/{action}:
    post:
      tags: [Offboarding]
      operationId: transitionOffboardingCase
      summary: Approve, start, complete or cancel an offboarding case
      description: >-
        Completing is refused while any blocking task is unfinished or the final settlement is
        not paid or waived, and cancels approved leave requests that start after the last working date.
      x-requirement: EMP-07.2
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/CaseId'
        - { name: action, in: path, required: true, schema: { type: string, enum: [approve, start, complete, cancel] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/DecisionRequest' } } } }
      responses:
        '200':
          description: Case state updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/OffboardingCase' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/offboarding/cases/{caseId}/tasks:
    get:
      tags: [Offboarding]
      operationId: listOffboardingTasks
      summary: List the handover and asset-recovery checklist of a case
      x-requirement: EMP-07.2
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/CaseId'
        - { name: category, in: query, schema: { $ref: '#/components/schemas/OffboardingTaskCategory' } }
        - { name: blockingOnly, in: query, schema: { type: boolean } }
      responses:
        '200': { description: Checklist tasks, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/OffboardingTask' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/offboarding/tasks/{taskId}/{action}:
    post:
      tags: [Offboarding]
      operationId: transitionOffboardingTask
      summary: Start, complete or reopen an offboarding checklist task
      x-requirement: EMP-07.2
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/TaskId'
        - { name: action, in: path, required: true, schema: { type: string, enum: [start, complete, reopen] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/DecisionRequest' } } } }
      responses:
        '200':
          description: Task updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/OffboardingTask' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }

  /api/v1/administration/users:
    get:
      tags: [Administration Users]
      operationId: listAdministrationUsers
      summary: Search local actor mappings without exposing IdP credentials
      x-requirement: SYS-01
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/Search'
        - { name: status, in: query, schema: { $ref: '#/components/schemas/UserStatus' } }
      responses:
        '200': { description: Paged users, content: { application/json: { schema: { $ref: '#/components/schemas/AdministrationUserPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [Administration Users]
      operationId: createAdministrationUserMapping
      summary: Map an existing IdP subject to a local QLNS actor
      description: Authentication and password lifecycle remain owned by the identity provider.
      x-requirement: SYS-01
      x-implementation-status: proposed
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/AdministrationUserWrite' } } } }
      responses:
        '201':
          description: User mapping created
          headers: { Location: { $ref: '#/components/headers/Location' }, ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/AdministrationUser' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/administration/users/{userId}:
    get:
      tags: [Administration Users]
      operationId: getAdministrationUser
      summary: Get a local actor mapping and role grants
      x-requirement: SYS-01
      x-implementation-status: proposed
      parameters: [{ $ref: '#/components/parameters/UserId' }]
      responses:
        '200':
          description: User mapping
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/AdministrationUser' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/administration/users/{userId}/{action}:
    post:
      tags: [Administration Users]
      operationId: transitionAdministrationUser
      summary: Enable, disable or synchronize a local actor mapping
      x-requirement: SYS-01
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/UserId'
        - { name: action, in: path, required: true, schema: { type: string, enum: [enable, disable, synchronize] } }
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/ReasonRequest' } } } }
      responses:
        '200':
          description: User mapping updated
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/AdministrationUser' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }

  /api/v1/administration/roles:
    get:
      tags: [Administration Authorization]
      operationId: listRoles
      summary: List assignable roles and permission descriptions
      x-requirement: SYS-01
      x-implementation-status: proposed
      responses:
        '200': { description: Role catalog, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/RoleDefinition' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/administration/users/{userId}/role-grants:
    get:
      tags: [Administration Authorization]
      operationId: listUserRoleGrants
      summary: List role and data-scope grants for one user
      x-requirement: SYS-01
      x-implementation-status: proposed
      parameters: [{ $ref: '#/components/parameters/UserId' }]
      responses:
        '200': { description: Role grants, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/RoleGrant' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    post:
      tags: [Administration Authorization]
      operationId: grantUserRole
      summary: Grant a role with an explicit data scope
      x-requirement: SYS-01
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/UserId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/RoleGrantWrite' } } } }
      responses:
        '201': { description: Role granted, content: { application/json: { schema: { $ref: '#/components/schemas/RoleGrant' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/administration/users/{userId}/role-grants/{roleCode}:
    delete:
      tags: [Administration Authorization]
      operationId: revokeUserRole
      summary: Revoke one exact role and data-scope grant
      x-requirement: SYS-01
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/UserId'
        - { name: roleCode, in: path, required: true, schema: { type: string, maxLength: 80 } }
        - { name: dataScopeType, in: query, required: true, schema: { $ref: '#/components/schemas/DataScopeType' } }
        - { name: dataScopeId, in: query, required: true, schema: { type: integer, format: int64, minimum: 0 } }
      responses:
        '204': { description: Role grant revoked }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }

  /api/v1/administration/audit-logs:
    get:
      tags: [Administration Audit]
      operationId: listAuditLogs
      summary: Search immutable audit records within the auditor's mandate
      x-requirement: SYS-03
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - { name: actorUserId, in: query, schema: { type: integer, format: int64, minimum: 1 } }
        - { name: action, in: query, schema: { type: string, maxLength: 120 } }
        - { name: entityType, in: query, schema: { type: string, maxLength: 100 } }
        - { name: entityId, in: query, schema: { type: string, maxLength: 100 } }
        - { name: result, in: query, schema: { $ref: '#/components/schemas/AuditResult' } }
        - { name: from, in: query, schema: { type: string, format: date-time } }
        - { name: to, in: query, schema: { type: string, format: date-time } }
      responses:
        '200': { description: Paged redacted audit records, content: { application/json: { schema: { $ref: '#/components/schemas/AuditLogPage' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/administration/audit-logs/{auditLogId}:
    get:
      tags: [Administration Audit]
      operationId: getAuditLog
      summary: Get one redacted audit record
      x-requirement: SYS-03
      x-implementation-status: proposed
      parameters: [{ $ref: '#/components/parameters/AuditLogId' }]
      responses:
        '200': { description: Audit record, content: { application/json: { schema: { $ref: '#/components/schemas/AuditLog' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/administration/deliveries:
    get:
      tags: [Administration Deliveries]
      operationId: listDeliveries
      summary: Search provider delivery and retry state
      x-requirement: SYS-02
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - { name: status, in: query, schema: { $ref: '#/components/schemas/DeliveryStatus' } }
        - { name: messageType, in: query, schema: { type: string, maxLength: 200 } }
        - { name: aggregateType, in: query, schema: { type: string, maxLength: 100 } }
        - { name: aggregateId, in: query, schema: { type: string, maxLength: 100 } }
      responses:
        '200': { description: Paged delivery records, content: { application/json: { schema: { $ref: '#/components/schemas/DeliveryPage' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/administration/deliveries/{deliveryId}/retry:
    post:
      tags: [Administration Deliveries]
      operationId: retryDelivery
      summary: Schedule a failed or dead-letter delivery for bounded retry
      x-requirement: SYS-02
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/DeliveryId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/ReasonRequest' } } } }
      responses:
        '202': { description: Retry scheduled, content: { application/json: { schema: { $ref: '#/components/schemas/Delivery' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }

  /api/v1/administration/integrations:
    get:
      tags: [Administration Integrations]
      operationId: listIntegrations
      summary: List integration configuration without returning secrets
      x-requirement: SYS-04
      x-implementation-status: proposed
      responses:
        '200': { description: Integrations, content: { application/json: { schema: { type: array, items: { $ref: '#/components/schemas/IntegrationConfiguration' } } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/administration/integrations/{integrationKey}:
    put:
      tags: [Administration Integrations]
      operationId: replaceIntegrationConfiguration
      summary: Replace non-secret settings and optionally rotate a write-only secret
      x-requirement: SYS-04
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/IntegrationKey'
        - $ref: '#/components/parameters/IfMatch'
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/IntegrationConfigurationWrite' } } } }
      responses:
        '200':
          description: Integration updated without returning secret material
          headers: { ETag: { $ref: '#/components/headers/ETag' } }
          content: { application/json: { schema: { $ref: '#/components/schemas/IntegrationConfiguration' } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/administration/integrations/{integrationKey}/test:
    post:
      tags: [Administration Integrations]
      operationId: testIntegration
      summary: Test an integration without persisting business changes
      x-requirement: SYS-04
      x-implementation-status: proposed
      parameters:
        - $ref: '#/components/parameters/IntegrationKey'
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '202': { description: Test accepted, content: { application/json: { schema: { $ref: '#/components/schemas/IntegrationTestResult' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /health/live:
    get:
      tags: [Operations]
      operationId: getLiveness
      summary: Return process liveness without dependency or secret details
      x-requirement: SYS-04
      x-implementation-status: proposed
      security: []
      responses:
        '200': { description: Process is alive, content: { application/json: { schema: { $ref: '#/components/schemas/HealthStatus' } } } }

  /health/ready:
    get:
      tags: [Operations]
      operationId: getReadiness
      summary: Return aggregate readiness without exposing dependency details
      x-requirement: SYS-04
      x-implementation-status: proposed
      security: []
      responses:
        '200': { description: Service is ready, content: { application/json: { schema: { $ref: '#/components/schemas/HealthStatus' } } } }
        '503': { description: Service is not ready, content: { application/json: { schema: { $ref: '#/components/schemas/HealthStatus' } } } }

components:
  securitySchemes:
    bearerAuth: { type: http, scheme: bearer, bearerFormat: JWT, description: OIDC access token for workforce users. }
    offerToken: { type: apiKey, in: header, name: X-Offer-Token, description: Short-lived single-offer candidate token. }
    deviceAuth: { type: apiKey, in: header, name: X-Device-Signature, description: Rotated device credential plus request signature verified by the attendance adapter. }
  parameters:
    Page: { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
    PageSize: { name: pageSize, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
    Search: { name: search, in: query, schema: { type: string, minLength: 1, maxLength: 200 } }
    IfMatch: { name: If-Match, in: header, required: true, schema: { type: string, pattern: '^"[1-9][0-9]*"$' }, example: '"4"' }
    IdempotencyKey: { name: Idempotency-Key, in: header, required: true, schema: { type: string, minLength: 16, maxLength: 128 } }
    RequisitionId: { name: requisitionId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    IntakeId: { name: intakeId, in: path, required: true, schema: { type: string, format: uuid } }
    ApplicationId: { name: applicationId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    InterviewId: { name: interviewId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    EvaluationId: { name: evaluationId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    OfferId: { name: offerId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    EmployeeId: { name: employeeId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    DepartmentId: { name: departmentId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    PositionId: { name: positionId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    TaskId: { name: taskId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    EventId: { name: eventId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    DocumentId: { name: documentId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    ContractId: { name: contractId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    AddendumId: { name: addendumId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    ExportId: { name: exportId, in: path, required: true, schema: { type: string, format: uuid } }
    ShiftId: { name: shiftId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    HolidayId: { name: holidayId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    OvertimeRequestId: { name: overtimeRequestId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    PeriodId: { name: periodId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    LeaveTypeId: { name: leaveTypeId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    ReviewId: { name: reviewId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    CaseId: { name: caseId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    CorrectionId: { name: correctionId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    LeaveRequestId: { name: leaveRequestId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    UserId: { name: userId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    AuditLogId: { name: auditLogId, in: path, required: true, schema: { type: integer, format: int64, minimum: 1 } }
    DeliveryId: { name: deliveryId, in: path, required: true, schema: { type: string, format: uuid } }
    IntegrationKey: { name: integrationKey, in: path, required: true, schema: { type: string, pattern: '^[a-z][a-z0-9_-]{1,79}$' } }
    ReportFrom: { name: from, in: query, required: true, schema: { type: string, format: date } }
    ReportTo: { name: to, in: query, required: true, schema: { type: string, format: date } }
    WeekStart: { name: weekStart, in: query, required: true, description: Monday starting the requested work week., schema: { type: string, format: date } }
  headers:
    ETag: { description: Quoted numeric resource version., schema: { type: string, example: '"5"' } }
    Location: { description: Created resource URI., schema: { type: string, format: uri-reference } }
  responses:
    BadRequest: { description: Malformed request or If-Match header, content: { application/problem+json: { schema: { $ref: '#/components/schemas/ProblemDetails' } } } }
    Unauthorized: { description: Authentication is required or invalid, content: { application/problem+json: { schema: { $ref: '#/components/schemas/ProblemDetails' } } } }
    Forbidden: { description: Insufficient permission, field access or data scope, content: { application/problem+json: { schema: { $ref: '#/components/schemas/ProblemDetails' } } } }
    NotFound: { description: Resource is not visible or does not exist, content: { application/problem+json: { schema: { $ref: '#/components/schemas/ProblemDetails' } } } }
    Conflict: { description: Workflow, prerequisite, uniqueness or concurrency conflict, content: { application/problem+json: { schema: { $ref: '#/components/schemas/BusinessProblemDetails' } } } }
    UnprocessableEntity: { description: Semantically invalid fields, content: { application/problem+json: { schema: { $ref: '#/components/schemas/ValidationProblemDetails' } } } }
    PayloadTooLarge: { description: File exceeds its documented limit, content: { application/problem+json: { schema: { $ref: '#/components/schemas/ProblemDetails' } } } }
    UnsupportedMediaType: { description: File type is not allowed, content: { application/problem+json: { schema: { $ref: '#/components/schemas/ProblemDetails' } } } }
  schemas:
    PageMetadata:
      type: object
      required: [page, pageSize, totalItems, totalPages]
      properties: { page: { type: integer, minimum: 1 }, pageSize: { type: integer, minimum: 1 }, totalItems: { type: integer, format: int64, minimum: 0 }, totalPages: { type: integer, minimum: 0 } }
    ReasonRequest: { type: object, additionalProperties: false, required: [reason], properties: { reason: { type: string, minLength: 1, maxLength: 1000 } } }
    ProblemDetails:
      type: object
      additionalProperties: true
      required: [type, title, status, correlationId]
      properties: { type: { type: string, format: uri }, title: { type: string }, status: { type: integer, minimum: 400, maximum: 599 }, detail: { type: string }, instance: { type: string }, correlationId: { type: string }, code: { type: string } }
    BusinessProblemDetails:
      allOf: [{ $ref: '#/components/schemas/ProblemDetails' }, { type: object, required: [code], properties: { code: { type: string } } }]
    ValidationProblemDetails:
      allOf: [{ $ref: '#/components/schemas/ProblemDetails' }, { type: object, required: [errors], properties: { errors: { type: object, additionalProperties: { type: array, items: { type: string } } } } }]

    RequisitionStatus: { type: string, enum: [draft, pending_approval, approved, active_recruiting, closed, cancelled] }
    EmploymentType: { type: string, enum: [full_time, part_time, hybrid, remote, internship, service_contract] }
    RequisitionWrite:
      type: object
      required: [title, departmentId, employmentType, targetHeadcount]
      properties:
        title: { type: string, minLength: 1, maxLength: 255 }
        departmentId: { type: integer, format: int64, minimum: 1 }
        positionId: { type: integer, format: int64, minimum: 1, nullable: true }
        description: { type: string, maxLength: 20000, nullable: true }
        requirements: { type: string, maxLength: 20000, nullable: true }
        location: { type: string, maxLength: 255, nullable: true }
        employmentType: { $ref: '#/components/schemas/EmploymentType' }
        salaryMin: { type: number, minimum: 0, nullable: true }
        salaryMax: { type: number, minimum: 0, nullable: true }
        targetHeadcount: { type: integer, minimum: 1 }
        closingDate: { type: string, format: date, nullable: true }
    Requisition:
      allOf:
        - $ref: '#/components/schemas/RequisitionWrite'
        - type: object
          required: [id, jobCode, status, version, createdAt, updatedAt]
          properties: { id: { type: integer, format: int64 }, jobCode: { type: string }, status: { $ref: '#/components/schemas/RequisitionStatus' }, publishedAt: { type: string, format: date-time, nullable: true }, createdBy: { type: integer, format: int64 }, version: { type: integer, format: int64, minimum: 1 }, createdAt: { type: string, format: date-time }, updatedAt: { type: string, format: date-time } }
    RequisitionAction:
      type: object
      additionalProperties: false
      properties: { reason: { type: string, minLength: 1, maxLength: 1000, nullable: true }, channels: { type: array, minItems: 1, uniqueItems: true, items: { type: string, enum: [careers_portal, linkedin, topcv] } } }
    RequisitionPage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/Requisition' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }

    CandidateInput:
      type: object
      required: [firstName, lastName, email]
      properties: { firstName: { type: string, maxLength: 100 }, lastName: { type: string, maxLength: 100 }, email: { type: string, format: email, maxLength: 320 }, phone: { type: string, maxLength: 30, nullable: true }, linkedinUrl: { type: string, format: uri, nullable: true }, portfolioUrl: { type: string, format: uri, nullable: true } }
    CandidateSummary:
      allOf: [{ $ref: '#/components/schemas/CandidateInput' }, { type: object, required: [id], properties: { id: { type: integer, format: int64 } } }]
    ResumeUpload:
      type: object
      additionalProperties: false
      required: [file, requisitionId, privacyNoticeVersion, consented]
      properties: { file: { type: string, format: binary }, requisitionId: { type: integer, format: int64, minimum: 1 }, privacyNoticeVersion: { type: string, maxLength: 50 }, consented: { type: boolean, enum: [true] } }
    CandidateIntake:
      type: object
      required: [id, requisitionId, status, uploadedAt]
      properties: { id: { type: string, format: uuid }, requisitionId: { type: integer, format: int64 }, status: { type: string, enum: [scanning, parsing, awaiting_confirmation, duplicate_review, completed, rejected, failed] }, parsedCandidate: { $ref: '#/components/schemas/CandidateInput' }, confidence: { type: object, additionalProperties: { type: number, minimum: 0, maximum: 1 } }, duplicateCandidates: { type: array, items: { $ref: '#/components/schemas/CandidateSummary' } }, uploadedAt: { type: string, format: date-time } }
    ConfirmIntake:
      type: object
      additionalProperties: false
      required: [candidate]
      properties: { candidate: { $ref: '#/components/schemas/CandidateInput' }, existingCandidateId: { type: integer, format: int64, minimum: 1, nullable: true }, source: { type: string, maxLength: 80, default: direct } }

    RecruitmentStage: { type: string, enum: [sourced_applied, ai_screening, tech_interview, executive_round, offer_letter, hired_ready, rejected, withdrawn] }
    ActiveRecruitmentStage: { type: string, enum: [sourced_applied, ai_screening, tech_interview, executive_round, offer_letter, hired_ready] }
    RecruitmentApplication:
      type: object
      required: [id, candidateId, jobPostingId, stage, source, appliedAt, version, updatedAt]
      properties: { id: { type: integer, format: int64 }, candidateId: { type: integer, format: int64 }, jobPostingId: { type: integer, format: int64 }, resumeId: { type: integer, format: int64, nullable: true }, stage: { $ref: '#/components/schemas/RecruitmentStage' }, aiScore: { type: number, minimum: 0, maximum: 100, nullable: true }, source: { type: string }, appliedAt: { type: string, format: date-time }, version: { type: integer, format: int64, minimum: 1 }, updatedAt: { type: string, format: date-time } }
    RecruitmentApplicationDetail:
      allOf: [{ $ref: '#/components/schemas/RecruitmentApplication' }, { type: object, required: [candidate], properties: { candidate: { $ref: '#/components/schemas/CandidateSummary' }, requisition: { $ref: '#/components/schemas/Requisition' } } }]
    AdvanceApplicationRequest: { type: object, additionalProperties: false, required: [targetStage], properties: { targetStage: { $ref: '#/components/schemas/ActiveRecruitmentStage' }, reason: { type: string, maxLength: 1000, nullable: true } } }
    PipelineCard: { type: object, required: [application, candidate], properties: { application: { $ref: '#/components/schemas/RecruitmentApplication' }, candidate: { $ref: '#/components/schemas/CandidateSummary' } } }
    PipelineColumn: { type: object, required: [stage, totalItems, items], properties: { stage: { $ref: '#/components/schemas/RecruitmentStage' }, totalItems: { type: integer, minimum: 0 }, averageAiScore: { type: number, nullable: true }, items: { type: array, items: { $ref: '#/components/schemas/PipelineCard' } } } }
    RecruitmentPipeline: { type: object, required: [requisitionId, columns], properties: { requisitionId: { type: integer, format: int64 }, columns: { type: array, items: { $ref: '#/components/schemas/PipelineColumn' } } } }

    InterviewStatus: { type: string, enum: [scheduled, completed, cancelled, no_show] }
    InterviewWrite:
      type: object
      required: [applicationId, interviewType, startsAt, endsAt, timezone, interviewerUserIds]
      properties: { applicationId: { type: integer, format: int64, minimum: 1 }, interviewType: { type: string, maxLength: 50 }, startsAt: { type: string, format: date-time }, endsAt: { type: string, format: date-time }, timezone: { type: string, example: Asia/Ho_Chi_Minh }, interviewerUserIds: { type: array, minItems: 1, uniqueItems: true, items: { type: integer, format: int64, minimum: 1 } }, location: { type: string, nullable: true }, meetingUrl: { type: string, format: uri, nullable: true } }
    Interview:
      allOf: [{ $ref: '#/components/schemas/InterviewWrite' }, { type: object, required: [id, status, version], properties: { id: { type: integer, format: int64 }, status: { $ref: '#/components/schemas/InterviewStatus' }, cancellationReason: { type: string, nullable: true }, version: { type: integer, format: int64, minimum: 1 } } }]
    InterviewAction: { type: object, additionalProperties: false, properties: { startsAt: { type: string, format: date-time }, endsAt: { type: string, format: date-time }, timezone: { type: string }, location: { type: string }, meetingUrl: { type: string, format: uri }, reason: { type: string, maxLength: 1000 } } }
    InterviewPage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/Interview' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }
    Score: { type: number, format: float, minimum: 0, maximum: 5, multipleOf: 0.5 }
    EvaluationWrite:
      type: object
      required: [technicalScore, communicationScore, problemSolvingScore, teamworkScore, recommendation, feedback]
      properties: { technicalScore: { $ref: '#/components/schemas/Score' }, communicationScore: { $ref: '#/components/schemas/Score' }, problemSolvingScore: { $ref: '#/components/schemas/Score' }, teamworkScore: { $ref: '#/components/schemas/Score' }, recommendation: { type: string, enum: [strong_hire, hire, hold, no_hire, strong_no_hire] }, feedback: { type: string, minLength: 1, maxLength: 10000 } }
    Evaluation:
      allOf: [{ $ref: '#/components/schemas/EvaluationWrite' }, { type: object, required: [id, interviewId, evaluatorUserId, overallScore, submittedAt, version], properties: { id: { type: integer, format: int64 }, interviewId: { type: integer, format: int64 }, evaluatorUserId: { type: integer, format: int64 }, overallScore: { type: number, minimum: 0, maximum: 5 }, submittedAt: { type: string, format: date-time }, unlockedAt: { type: string, format: date-time, nullable: true }, version: { type: integer, minimum: 1 } } }]

    OfferStatus: { type: string, enum: [draft, approved, sent, accepted, declined, expired, cancelled] }
    OfferWrite:
      type: object
      required: [applicationId, baseSalary, employmentType, startDate, expirationDate, templateVersion]
      properties: { applicationId: { type: integer, format: int64, minimum: 1 }, baseSalary: { type: number, minimum: 0 }, bonusAmount: { type: number, minimum: 0, nullable: true }, allowanceAmount: { type: number, minimum: 0, nullable: true }, currency: { type: string, pattern: '^[A-Z]{3}$', default: VND }, employmentType: { $ref: '#/components/schemas/EmploymentType' }, startDate: { type: string, format: date }, expirationDate: { type: string, format: date }, templateVersion: { type: string, maxLength: 50 } }
    Offer:
      allOf: [{ $ref: '#/components/schemas/OfferWrite' }, { type: object, required: [id, status, version, createdAt, updatedAt], properties: { id: { type: integer, format: int64 }, status: { $ref: '#/components/schemas/OfferStatus' }, documentAvailable: { type: boolean }, approvedBy: { type: integer, format: int64, nullable: true }, approvedAt: { type: string, format: date-time, nullable: true }, sentAt: { type: string, format: date-time, nullable: true }, respondedAt: { type: string, format: date-time, nullable: true }, version: { type: integer, format: int64 }, createdAt: { type: string, format: date-time }, updatedAt: { type: string, format: date-time } } }]
    OfferAction: { type: object, additionalProperties: false, properties: { reason: { type: string, maxLength: 1000 }, expirationDate: { type: string, format: date } } }
    OfferResponse: { type: object, additionalProperties: false, required: [decision], properties: { decision: { type: string, enum: [accept, decline] }, reason: { type: string, maxLength: 1000, nullable: true } } }
    OfferResponseResult: { type: object, required: [offerId, status, replayed], properties: { offerId: { type: integer, format: int64 }, status: { type: string, enum: [accepted, declined] }, employeeId: { type: integer, format: int64, nullable: true }, initialContractId: { type: integer, format: int64, nullable: true }, onboardingTaskIds: { type: array, items: { type: integer, format: int64 } }, replayed: { type: boolean } } }
    OfferPage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/Offer' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }

    EmployeeStatus: { type: string, enum: [probation, active, suspended, terminated] }
    EmployeeSummary:
      type: object
      required: [id, employeeCode, firstName, lastName, workEmail, departmentId, positionId, status]
      properties: { id: { type: integer, format: int64 }, employeeCode: { type: string }, firstName: { type: string }, lastName: { type: string }, workEmail: { type: string, format: email }, departmentId: { type: integer, format: int64 }, positionId: { type: integer, format: int64 }, managerId: { type: integer, format: int64, nullable: true }, officeLocation: { type: string, nullable: true }, status: { $ref: '#/components/schemas/EmployeeStatus' } }
    EmployeeDetail:
      allOf: [{ $ref: '#/components/schemas/EmployeeSummary' }, { type: object, required: [hireDate, version, createdAt, updatedAt], properties: { personalEmail: { type: string, format: email, nullable: true }, phone: { type: string, nullable: true }, dateOfBirth: { type: string, format: date, nullable: true }, gender: { type: string, nullable: true }, permanentAddress: { type: string, nullable: true }, temporaryAddress: { type: string, nullable: true }, emergencyContact: { type: object, nullable: true, additionalProperties: { type: string } }, hireDate: { type: string, format: date }, version: { type: integer, format: int64 }, createdAt: { type: string, format: date-time }, updatedAt: { type: string, format: date-time } } }]
    PersonalProfilePatch: { type: object, additionalProperties: false, minProperties: 1, properties: { personalEmail: { type: string, format: email, nullable: true }, phone: { type: string, maxLength: 30, nullable: true }, temporaryAddress: { type: string, maxLength: 1000, nullable: true }, emergencyContact: { type: object, nullable: true, additionalProperties: { type: string } } } }
    EmployeePage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/EmployeeSummary' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }
    DepartmentWrite: { type: object, required: [code, name], properties: { code: { type: string, maxLength: 50 }, name: { type: string, maxLength: 255 }, parentDepartmentId: { type: integer, format: int64, nullable: true }, costCenter: { type: string, maxLength: 100, nullable: true }, description: { type: string, maxLength: 5000, nullable: true } } }
    Department:
      allOf: [{ $ref: '#/components/schemas/DepartmentWrite' }, { type: object, required: [id, headcount, version], properties: { id: { type: integer, format: int64 }, manager: { $ref: '#/components/schemas/EmployeeSummary' }, headcount: { type: integer, minimum: 0 }, version: { type: integer, format: int64 } } }]
    PositionWrite: { type: object, required: [code, name], properties: { code: { type: string, maxLength: 50 }, name: { type: string, maxLength: 255 }, level: { type: string, maxLength: 50, nullable: true }, description: { type: string, maxLength: 5000, nullable: true } } }
    Position:
      allOf: [{ $ref: '#/components/schemas/PositionWrite' }, { type: object, required: [id, version], properties: { id: { type: integer, format: int64 }, version: { type: integer, format: int64 }, createdAt: { type: string, format: date-time }, updatedAt: { type: string, format: date-time } } }]
    OrganizationNode:
      allOf: [{ $ref: '#/components/schemas/Department' }, { type: object, required: [children], properties: { children: { type: array, items: { $ref: '#/components/schemas/OrganizationNode' } } } }]

    OnboardingTaskStatus: { type: string, enum: [pending, in_progress, completed] }
    OnboardingTaskWrite: { type: object, additionalProperties: false, required: [taskName], properties: { taskName: { type: string, maxLength: 255 }, description: { type: string, maxLength: 5000, nullable: true }, assignedToUserId: { type: integer, format: int64, minimum: 1, nullable: true }, dueAt: { type: string, format: date-time, nullable: true } } }
    OnboardingTask: { type: object, required: [id, employeeId, templateKey, taskName, status, overdue, version], properties: { id: { type: integer, format: int64 }, employeeId: { type: integer, format: int64 }, templateKey: { type: string }, taskName: { type: string }, description: { type: string, nullable: true }, assignedToUserId: { type: integer, format: int64, nullable: true }, dueAt: { type: string, format: date-time, nullable: true }, status: { $ref: '#/components/schemas/OnboardingTaskStatus' }, completedAt: { type: string, format: date-time, nullable: true }, overdue: { type: boolean }, version: { type: integer, format: int64 } } }
    OnboardingTaskPage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/OnboardingTask' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }
    EmployeeEventWrite: { type: object, required: [eventType, effectiveDate, beforeData, afterData, reason], properties: { eventType: { type: string, enum: [promotion, transfer, demotion, salary_adjustment, termination, correction] }, effectiveDate: { type: string, format: date }, beforeData: { type: object, additionalProperties: true }, afterData: { type: object, additionalProperties: true }, reason: { type: string, maxLength: 5000 }, compensatesEventId: { type: integer, format: int64, nullable: true } } }
    EmployeeEvent:
      allOf: [{ $ref: '#/components/schemas/EmployeeEventWrite' }, { type: object, required: [id, employeeId, status, createdBy, version], properties: { id: { type: integer, format: int64 }, employeeId: { type: integer, format: int64 }, status: { type: string, enum: [draft, pending_approval, approved, applied, cancelled] }, createdBy: { type: integer, format: int64 }, approvedBy: { type: integer, format: int64, nullable: true }, appliedAt: { type: string, format: date-time, nullable: true }, version: { type: integer, format: int64 } } }]
    EmployeeEventPage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/EmployeeEvent' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }
    EmployeeDocument: { type: object, required: [id, employeeId, documentType, version, originalFileName, contentType, sizeBytes, uploadedAt], properties: { id: { type: integer, format: int64 }, employeeId: { type: integer, format: int64 }, documentType: { type: string }, version: { type: integer, minimum: 1 }, originalFileName: { type: string }, contentType: { type: string }, sizeBytes: { type: integer, format: int64 }, retentionUntil: { type: string, format: date, nullable: true }, uploadedAt: { type: string, format: date-time } } }
    EmployeeDocumentUpload: { type: object, additionalProperties: false, required: [file, documentType], properties: { file: { type: string, format: binary }, documentType: { type: string, maxLength: 80 }, retentionUntil: { type: string, format: date, nullable: true } } }
    SignedDownload: { type: object, required: [url, expiresAt], properties: { url: { type: string, format: uri }, expiresAt: { type: string, format: date-time } } }

    ContractType: { type: string, enum: [probation, fixed_term, indefinite, internship, service_contract] }
    ContractStatus: { type: string, enum: [draft, approved, executed, active, expired, terminated, cancelled] }
    ContractWrite:
      type: object
      required: [employeeId, contractNumber, contractType, startDate, salary, isPrimary]
      properties: { employeeId: { type: integer, format: int64, minimum: 1 }, contractNumber: { type: string, maxLength: 100 }, contractType: { $ref: '#/components/schemas/ContractType' }, startDate: { type: string, format: date }, endDate: { type: string, format: date, nullable: true }, salary: { type: number, minimum: 0 }, currency: { type: string, pattern: '^[A-Z]{3}$', default: VND }, noticePeriodDays: { type: integer, minimum: 0, nullable: true }, isPrimary: { type: boolean, default: true } }
    Contract:
      allOf: [{ $ref: '#/components/schemas/ContractWrite' }, { type: object, required: [id, status, signedDocumentAvailable, version], properties: { id: { type: integer, format: int64 }, status: { $ref: '#/components/schemas/ContractStatus' }, signedDocumentAvailable: { type: boolean }, signedAt: { type: string, format: date-time, nullable: true }, version: { type: integer, format: int64 }, createdAt: { type: string, format: date-time }, updatedAt: { type: string, format: date-time } } }]
    ContractAction: { type: object, additionalProperties: false, properties: { reason: { type: string, maxLength: 1000 }, signedAt: { type: string, format: date-time }, allowPrimaryOverlap: { type: boolean, default: false } } }
    ContractPage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/Contract' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }
    ExpiringContract:
      allOf: [{ $ref: '#/components/schemas/Contract' }, { type: object, required: [daysRemaining, alertLevel], properties: { daysRemaining: { type: integer }, alertLevel: { type: string, enum: [amber, red] } } }]
    ExpiringContractPage: { type: object, required: [items, page, asOf], properties: { items: { type: array, items: { $ref: '#/components/schemas/ExpiringContract' } }, page: { $ref: '#/components/schemas/PageMetadata' }, asOf: { type: string, format: date } } }
    ContractAddendumWrite: { type: object, required: [addendumNumber, effectiveDate, beforeTerms, afterTerms, reason], properties: { addendumNumber: { type: string, maxLength: 100 }, effectiveDate: { type: string, format: date }, beforeTerms: { type: object, additionalProperties: true }, afterTerms: { type: object, additionalProperties: true }, reason: { type: string, maxLength: 5000 } } }
    ContractAddendum:
      allOf: [{ $ref: '#/components/schemas/ContractAddendumWrite' }, { type: object, required: [id, contractId, status, version, createdBy], properties: { id: { type: integer, format: int64 }, contractId: { type: integer, format: int64 }, status: { type: string, enum: [draft, pending_approval, approved, effective, superseded, cancelled] }, version: { type: integer, minimum: 1 }, createdBy: { type: integer, format: int64 }, approvedBy: { type: integer, format: int64, nullable: true }, approvedAt: { type: string, format: date-time, nullable: true }, signedAt: { type: string, format: date-time, nullable: true }, signedDocumentAvailable: { type: boolean } } }]

    ReportFilter:
      type: object
      required: [from, to]
      properties: { from: { type: string, format: date }, to: { type: string, format: date }, departmentId: { type: integer, format: int64, nullable: true }, positionId: { type: integer, format: int64, nullable: true }, location: { type: string, nullable: true } }
    ReportMetric:
      type: object
      required: [key, label, value, formula]
      properties: { key: { type: string }, label: { type: string }, value: { type: number }, unit: { type: string, enum: [count, percent, days, currency] }, formula: { type: string, description: Stable formula identifier rather than executable expression. } }
    DistributionPoint:
      type: object
      required: [key, label, value]
      properties: { key: { type: string }, label: { type: string }, value: { type: number }, percent: { type: number, minimum: 0, maximum: 100, nullable: true } }
    HeadcountReport:
      type: object
      required: [filter, metrics, byDepartment, byContractType, refreshedAt, policyVersion]
      properties: { filter: { $ref: '#/components/schemas/ReportFilter' }, metrics: { type: array, items: { $ref: '#/components/schemas/ReportMetric' } }, byDepartment: { type: array, items: { $ref: '#/components/schemas/DistributionPoint' } }, byContractType: { type: array, items: { $ref: '#/components/schemas/DistributionPoint' } }, refreshedAt: { type: string, format: date-time }, policyVersion: { type: string } }
    FunnelPoint:
      type: object
      required: [stage, enteredCount, conversionRate]
      properties: { stage: { $ref: '#/components/schemas/RecruitmentStage' }, enteredCount: { type: integer, minimum: 0 }, conversionRate: { type: number, minimum: 0, maximum: 100 } }
    RecruitmentReport:
      type: object
      required: [filter, funnel, metrics, bySource, refreshedAt, policyVersion]
      properties: { filter: { $ref: '#/components/schemas/ReportFilter' }, funnel: { type: array, items: { $ref: '#/components/schemas/FunnelPoint' } }, metrics: { type: array, items: { $ref: '#/components/schemas/ReportMetric' } }, bySource: { type: array, items: { $ref: '#/components/schemas/DistributionPoint' } }, refreshedAt: { type: string, format: date-time }, policyVersion: { type: string } }
    ReportType: { type: string, enum: [headcount, recruitment] }
    ExportFormat: { type: string, enum: [csv, xlsx, pdf] }
    CreateReportExportRequest:
      type: object
      additionalProperties: false
      required: [reportType, format, filter]
      properties: { reportType: { $ref: '#/components/schemas/ReportType' }, format: { $ref: '#/components/schemas/ExportFormat' }, filter: { $ref: '#/components/schemas/ReportFilter' } }
    ReportExport:
      type: object
      required: [id, reportType, format, status, requestedAt, expiresAt]
      properties: { id: { type: string, format: uuid }, reportType: { $ref: '#/components/schemas/ReportType' }, format: { $ref: '#/components/schemas/ExportFormat' }, status: { type: string, enum: [queued, processing, completed, failed, expired] }, requestedAt: { type: string, format: date-time }, completedAt: { type: string, format: date-time, nullable: true }, expiresAt: { type: string, format: date-time }, failureCode: { type: string, nullable: true } }

    LocalTime: { type: string, pattern: '^([01][0-9]|2[0-3]):[0-5][0-9]$', example: '08:30' }
    WorkShiftWrite:
      type: object
      required: [code, name, startsAt, endsAt, timezone, breakMinutes, standardWorkMinutes, workCoefficient, active]
      properties: { code: { type: string, maxLength: 50 }, name: { type: string, maxLength: 255 }, startsAt: { $ref: '#/components/schemas/LocalTime' }, endsAt: { $ref: '#/components/schemas/LocalTime' }, timezone: { type: string, example: Asia/Ho_Chi_Minh }, breakMinutes: { type: integer, minimum: 0, maximum: 1440 }, standardWorkMinutes: { type: integer, minimum: 1, maximum: 1440 }, workCoefficient: { type: number, minimum: 0, maximum: 5 }, active: { type: boolean } }
    WorkShift:
      allOf: [{ $ref: '#/components/schemas/WorkShiftWrite' }, { type: object, required: [id, crossesMidnight, version], properties: { id: { type: integer, format: int64 }, crossesMidnight: { type: boolean }, version: { type: integer, format: int64, minimum: 1 } } }]
    WorkScheduleAssignment:
      type: object
      required: [id, employeeId, workDate, shiftId, timezone, version]
      properties: { id: { type: integer, format: int64 }, employeeId: { type: integer, format: int64 }, workDate: { type: string, format: date }, shiftId: { type: integer, format: int64 }, timezone: { type: string }, version: { type: integer, format: int64, minimum: 1 } }
    WorkScheduleAssignmentWrite:
      type: object
      additionalProperties: false
      required: [employeeId, workDate, shiftId]
      properties: { employeeId: { type: integer, format: int64, minimum: 1 }, workDate: { type: string, format: date }, shiftId: { type: integer, format: int64, minimum: 1 } }
    ReplaceWorkScheduleRequest:
      type: object
      additionalProperties: false
      required: [assignments]
      properties: { assignments: { type: array, minItems: 1, maxItems: 1000, items: { $ref: '#/components/schemas/WorkScheduleAssignmentWrite' } } }
    WorkSchedulePage: { type: object, required: [items, page, weekStart], properties: { items: { type: array, items: { $ref: '#/components/schemas/WorkScheduleAssignment' } }, page: { $ref: '#/components/schemas/PageMetadata' }, weekStart: { type: string, format: date } } }
    AttendanceEventType: { type: string, enum: [check_in, check_out] }
    AttendanceMethod: { type: string, enum: [fingerprint, gps, face_id, wifi, web, manual] }
    AttendanceEventWrite:
      type: object
      required: [eventType, occurredAt, timezone, method]
      properties: { employeeId: { type: integer, format: int64, minimum: 1, nullable: true }, eventType: { $ref: '#/components/schemas/AttendanceEventType' }, occurredAt: { type: string, format: date-time }, timezone: { type: string }, method: { $ref: '#/components/schemas/AttendanceMethod' }, latitude: { type: number, minimum: -90, maximum: 90, nullable: true }, longitude: { type: number, minimum: -180, maximum: 180, nullable: true }, deviceId: { type: string, maxLength: 200, nullable: true } }
    DeviceAttendanceEventWrite:
      allOf: [{ $ref: '#/components/schemas/AttendanceEventWrite' }, { type: object, required: [externalEventId, employeeExternalKey, deviceId], properties: { externalEventId: { type: string, maxLength: 200 }, employeeExternalKey: { type: string, maxLength: 200 } } }]
    AttendanceEvent:
      allOf: [{ $ref: '#/components/schemas/AttendanceEventWrite' }, { type: object, required: [id, employeeId, recordedAt, duplicate], properties: { id: { type: string, format: uuid }, employeeId: { type: integer, format: int64 }, recordedAt: { type: string, format: date-time }, duplicate: { type: boolean } } }]
    TimesheetStatus: { type: string, enum: [on_time, late, early_leave, absent, incomplete, corrected, on_leave] }
    TimesheetRow:
      type: object
      required: [employeeId, workDate, status, scheduledMinutes, workedMinutes, policyVersion]
      properties: { employeeId: { type: integer, format: int64 }, workDate: { type: string, format: date }, shiftId: { type: integer, format: int64, nullable: true }, firstCheckInAt: { type: string, format: date-time, nullable: true }, lastCheckOutAt: { type: string, format: date-time, nullable: true }, status: { $ref: '#/components/schemas/TimesheetStatus' }, scheduledMinutes: { type: integer, minimum: 0 }, workedMinutes: { type: integer, minimum: 0 }, lateMinutes: { type: integer, minimum: 0 }, earlyLeaveMinutes: { type: integer, minimum: 0 }, policyVersion: { type: string } }
    TimesheetPage: { type: object, required: [items, page, from, to], properties: { items: { type: array, items: { $ref: '#/components/schemas/TimesheetRow' } }, page: { $ref: '#/components/schemas/PageMetadata' }, from: { type: string, format: date }, to: { type: string, format: date } } }
    ApprovalStatus: { type: string, enum: [pending, approved, rejected, cancelled] }
    DecisionRequest: { type: object, additionalProperties: false, properties: { reason: { type: string, maxLength: 1000, nullable: true } } }
    AttendanceCorrectionWrite:
      type: object
      required: [employeeId, workDate, proposedCheckInAt, proposedCheckOutAt, reason]
      properties: { employeeId: { type: integer, format: int64, minimum: 1 }, workDate: { type: string, format: date }, proposedCheckInAt: { type: string, format: date-time }, proposedCheckOutAt: { type: string, format: date-time }, reason: { type: string, minLength: 1, maxLength: 1000 } }
    AttendanceCorrection:
      allOf: [{ $ref: '#/components/schemas/AttendanceCorrectionWrite' }, { type: object, required: [id, status, requestedBy, version, createdAt], properties: { id: { type: integer, format: int64 }, status: { $ref: '#/components/schemas/ApprovalStatus' }, requestedBy: { type: integer, format: int64 }, decidedBy: { type: integer, format: int64, nullable: true }, decisionReason: { type: string, nullable: true }, version: { type: integer, format: int64 }, createdAt: { type: string, format: date-time } } }]
    AttendanceCorrectionPage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/AttendanceCorrection' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }

    LeaveUnit: { type: string, enum: [full_day, first_half, second_half, hours] }
    LeaveType:
      type: object
      required: [id, code, name, paid, allowNegativeBalance, policyVersion, active]
      properties: { id: { type: integer, format: int64 }, code: { type: string }, name: { type: string }, paid: { type: boolean }, allowNegativeBalance: { type: boolean }, policyVersion: { type: string }, active: { type: boolean } }
    LeaveBalance:
      type: object
      required: [employeeId, leaveTypeId, year, entitledUnits, usedUnits, reservedUnits, availableUnits, unit, policyVersion]
      properties: { employeeId: { type: integer, format: int64 }, leaveTypeId: { type: integer, format: int64 }, year: { type: integer }, entitledUnits: { type: number }, usedUnits: { type: number }, reservedUnits: { type: number }, availableUnits: { type: number }, unit: { type: string, enum: [days, hours] }, policyVersion: { type: string } }
    LeaveRequestWrite:
      type: object
      required: [employeeId, leaveTypeId, startsAt, endsAt, timezone, unit, reason]
      properties: { employeeId: { type: integer, format: int64, minimum: 1 }, leaveTypeId: { type: integer, format: int64, minimum: 1 }, startsAt: { type: string, format: date-time }, endsAt: { type: string, format: date-time }, timezone: { type: string }, unit: { $ref: '#/components/schemas/LeaveUnit' }, reason: { type: string, minLength: 1, maxLength: 2000 } }
    LeaveDecision:
      type: object
      required: [sequence, approverUserId, decision, decidedAt]
      properties: { sequence: { type: integer, minimum: 1 }, approverUserId: { type: integer, format: int64 }, decision: { type: string, enum: [approved, rejected] }, reason: { type: string, nullable: true }, decidedAt: { type: string, format: date-time } }
    LeaveRequest:
      allOf: [{ $ref: '#/components/schemas/LeaveRequestWrite' }, { type: object, required: [id, requestCode, status, requestedUnits, unitOfBalance, policyVersion, decisions, version, createdAt], properties: { id: { type: integer, format: int64 }, requestCode: { type: string }, status: { $ref: '#/components/schemas/ApprovalStatus' }, requestedUnits: { type: number, minimum: 0 }, unitOfBalance: { type: string, enum: [days, hours] }, policyVersion: { type: string }, decisions: { type: array, items: { $ref: '#/components/schemas/LeaveDecision' } }, version: { type: integer, format: int64 }, createdAt: { type: string, format: date-time } } }]
    LeaveRequestPage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/LeaveRequest' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }
    BatchLeaveDecisionItem: { type: object, additionalProperties: false, required: [leaveRequestId, expectedVersion], properties: { leaveRequestId: { type: integer, format: int64, minimum: 1 }, expectedVersion: { type: integer, format: int64, minimum: 1 } } }
    BatchLeaveDecisionRequest: { type: object, additionalProperties: false, required: [decision, items], properties: { decision: { type: string, enum: [approve, reject] }, reason: { type: string, maxLength: 1000, nullable: true }, items: { type: array, minItems: 1, maxItems: 100, items: { $ref: '#/components/schemas/BatchLeaveDecisionItem' } } } }
    BatchItemResult: { type: object, required: [leaveRequestId, status], properties: { leaveRequestId: { type: integer, format: int64 }, status: { type: integer }, code: { type: string, nullable: true }, newVersion: { type: integer, format: int64, nullable: true } } }
    BatchLeaveDecisionResult: { type: object, required: [results], properties: { results: { type: array, items: { $ref: '#/components/schemas/BatchItemResult' } } } }

    HolidayWrite:
      type: object
      additionalProperties: false
      required: [holidayDate, name, paid, workCoefficient]
      properties: { calendarCode: { type: string, maxLength: 50, default: VN }, holidayDate: { type: string, format: date }, name: { type: string, maxLength: 255 }, paid: { type: boolean }, workCoefficient: { type: number, minimum: 0, maximum: 5 } }
    Holiday:
      allOf: [{ $ref: '#/components/schemas/HolidayWrite' }, { type: object, required: [id, calendarCode, version], properties: { id: { type: integer, format: int64 }, version: { type: integer, format: int64, minimum: 1 } } }]
    OvertimeCategory: { type: string, enum: [weekday, weekly_rest, holiday, night] }
    OvertimeRequestWrite:
      type: object
      additionalProperties: false
      required: [employeeId, workDate, startsAt, endsAt, timezone, reason]
      properties: { employeeId: { type: integer, format: int64, minimum: 1 }, workDate: { type: string, format: date }, startsAt: { type: string, format: date-time }, endsAt: { type: string, format: date-time }, timezone: { type: string }, reason: { type: string, minLength: 1, maxLength: 2000 } }
    OvertimeRequest:
      allOf: [{ $ref: '#/components/schemas/OvertimeRequestWrite' }, { type: object, required: [id, requestCode, overtimeCategory, requestedMinutes, workCoefficient, status, policyVersion, version, createdAt], properties: { id: { type: integer, format: int64 }, requestCode: { type: string }, overtimeCategory: { $ref: '#/components/schemas/OvertimeCategory' }, requestedMinutes: { type: integer, minimum: 1 }, approvedMinutes: { type: integer, minimum: 0, nullable: true }, workCoefficient: { type: number, minimum: 1, maximum: 5 }, status: { $ref: '#/components/schemas/ApprovalStatus' }, requestedBy: { type: integer, format: int64 }, decidedBy: { type: integer, format: int64, nullable: true }, decisionReason: { type: string, nullable: true }, policyVersion: { type: string }, version: { type: integer, format: int64 }, createdAt: { type: string, format: date-time } } }]
    OvertimeRequestPage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/OvertimeRequest' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }
    OvertimeDecisionRequest:
      type: object
      additionalProperties: false
      properties: { approvedMinutes: { type: integer, minimum: 0, nullable: true, description: Must not exceed requestedMinutes. Defaults to requestedMinutes when omitted. }, reason: { type: string, maxLength: 1000, nullable: true } }
    TimesheetPeriodStatus: { type: string, enum: [open, pending_approval, approved, locked, reopened] }
    TimesheetPeriodWrite:
      type: object
      additionalProperties: false
      required: [periodCode, startsOn, endsOn]
      properties: { periodCode: { type: string, maxLength: 50, example: '2026-09' }, startsOn: { type: string, format: date }, endsOn: { type: string, format: date } }
    TimesheetPeriod:
      allOf: [{ $ref: '#/components/schemas/TimesheetPeriodWrite' }, { type: object, required: [id, status, version], properties: { id: { type: integer, format: int64 }, status: { $ref: '#/components/schemas/TimesheetPeriodStatus' }, reviewedBy: { type: integer, format: int64, nullable: true }, reviewedAt: { type: string, format: date-time, nullable: true }, approvedBy: { type: integer, format: int64, nullable: true }, approvedAt: { type: string, format: date-time, nullable: true }, lockedBy: { type: integer, format: int64, nullable: true }, lockedAt: { type: string, format: date-time, nullable: true }, reopenReason: { type: string, nullable: true }, payrollHandoffAt: { type: string, format: date-time, nullable: true }, payrollHandoffReference: { type: string, nullable: true }, openExceptionCount: { type: integer, minimum: 0 }, version: { type: integer, format: int64 } } }]
    TimesheetPeriodPage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/TimesheetPeriod' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }
    TimesheetExceptionKind: { type: string, enum: [incomplete_workday, unexplained_absence, pending_correction, overtime_without_attendance, missing_schedule] }
    TimesheetException:
      type: object
      required: [kind, employeeId, workDate, detail]
      properties: { kind: { $ref: '#/components/schemas/TimesheetExceptionKind' }, employeeId: { type: integer, format: int64 }, workDate: { type: string, format: date }, detail: { type: string }, relatedId: { type: integer, format: int64, nullable: true } }
    TimesheetExceptionPage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/TimesheetException' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }
    PayrollHandoffRequest:
      type: object
      additionalProperties: false
      required: [reference]
      properties: { reference: { type: string, minLength: 1, maxLength: 200 }, note: { type: string, maxLength: 1000, nullable: true } }
    LeaveTypeWrite:
      type: object
      additionalProperties: false
      required: [code, name, paid, unitOfBalance, allowNegativeBalance, allowHalfDay, countsHolidays, requiresAttachment, minNoticeDays, approvalLevels, policyVersion]
      properties: { code: { type: string, maxLength: 50 }, name: { type: string, maxLength: 255 }, paid: { type: boolean }, unitOfBalance: { type: string, enum: [days, hours] }, allowNegativeBalance: { type: boolean }, allowHalfDay: { type: boolean }, countsHolidays: { type: boolean }, requiresAttachment: { type: boolean }, minNoticeDays: { type: integer, minimum: 0 }, maxConsecutiveUnits: { type: number, minimum: 0, nullable: true }, approvalLevels: { type: integer, minimum: 1, maximum: 3 }, policyVersion: { type: string, maxLength: 50 } }
    ProbationReviewStatus: { type: string, enum: [pending, in_review, decided, cancelled] }
    ProbationOutcome: { type: string, enum: [confirmed, extended, terminated] }
    ProbationReviewWrite:
      type: object
      additionalProperties: false
      required: [overallScore, strengths, improvements, recommendedOutcome]
      properties: { overallScore: { type: number, minimum: 0, maximum: 5 }, strengths: { type: string, minLength: 1, maxLength: 4000 }, improvements: { type: string, maxLength: 4000, nullable: true, description: Required when recommendedOutcome is terminated. }, recommendedOutcome: { $ref: '#/components/schemas/ProbationOutcome' } }
    ProbationReview:
      type: object
      required: [id, employeeId, contractId, reviewDueDate, status, version, createdAt]
      properties: { id: { type: integer, format: int64 }, employeeId: { type: integer, format: int64 }, contractId: { type: integer, format: int64 }, reviewDueDate: { type: string, format: date }, overdue: { type: boolean }, reviewerUserId: { type: integer, format: int64, nullable: true }, status: { $ref: '#/components/schemas/ProbationReviewStatus' }, outcome: { $ref: '#/components/schemas/ProbationOutcome' }, overallScore: { type: number, nullable: true }, strengths: { type: string, nullable: true }, improvements: { type: string, nullable: true }, effectiveDate: { type: string, format: date, nullable: true }, decidedBy: { type: integer, format: int64, nullable: true }, decidedAt: { type: string, format: date-time, nullable: true }, employeeEventId: { type: integer, format: int64, nullable: true }, version: { type: integer, format: int64 }, createdAt: { type: string, format: date-time } }
    ProbationReviewPage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/ProbationReview' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }
    ProbationDecisionRequest:
      type: object
      additionalProperties: false
      properties: { outcome: { $ref: '#/components/schemas/ProbationOutcome' }, effectiveDate: { type: string, format: date }, reason: { type: string, maxLength: 1000, nullable: true } }
    SeparationType: { type: string, enum: [resignation, mutual_agreement, dismissal, contract_expiry, retirement] }
    OffboardingStatus: { type: string, enum: [draft, pending_approval, approved, in_progress, completed, cancelled] }
    FinalSettlementStatus: { type: string, enum: [pending, calculated, paid, waived] }
    OffboardingTaskCategory: { type: string, enum: [it, admin, hr, manager, finance] }
    OffboardingCaseWrite:
      type: object
      additionalProperties: false
      required: [employeeId, separationType, lastWorkingDate, reason]
      properties: { employeeId: { type: integer, format: int64, minimum: 1 }, separationType: { $ref: '#/components/schemas/SeparationType' }, noticeReceivedOn: { type: string, format: date, nullable: true }, lastWorkingDate: { type: string, format: date }, handoverToEmployeeId: { type: integer, format: int64, minimum: 1, nullable: true }, reason: { type: string, minLength: 1, maxLength: 4000 } }
    OffboardingCase:
      allOf: [{ $ref: '#/components/schemas/OffboardingCaseWrite' }, { type: object, required: [id, status, finalSettlementStatus, version, createdAt], properties: { id: { type: integer, format: int64 }, employeeEventId: { type: integer, format: int64, nullable: true }, exitInterviewAt: { type: string, format: date-time, nullable: true }, finalSettlementStatus: { $ref: '#/components/schemas/FinalSettlementStatus' }, status: { $ref: '#/components/schemas/OffboardingStatus' }, blockingTasksOutstanding: { type: integer, minimum: 0 }, noticePeriodShortfallDays: { type: integer, nullable: true, description: Positive when the notice period is shorter than the contract requires. Reported as a warning, not an error. }, approvedBy: { type: integer, format: int64, nullable: true }, approvedAt: { type: string, format: date-time, nullable: true }, completedAt: { type: string, format: date-time, nullable: true }, version: { type: integer, format: int64 }, createdAt: { type: string, format: date-time } } }]
    OffboardingCasePage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/OffboardingCase' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }
    OffboardingTask:
      type: object
      required: [id, offboardingCaseId, templateKey, category, taskName, status, blocksLastWorkingDay, version]
      properties: { id: { type: integer, format: int64 }, offboardingCaseId: { type: integer, format: int64 }, templateKey: { type: string }, category: { $ref: '#/components/schemas/OffboardingTaskCategory' }, taskName: { type: string }, description: { type: string, nullable: true }, assignedToUserId: { type: integer, format: int64, nullable: true }, dueAt: { type: string, format: date-time, nullable: true }, blocksLastWorkingDay: { type: boolean }, status: { type: string, enum: [pending, in_progress, completed] }, completedAt: { type: string, format: date-time, nullable: true }, version: { type: integer, format: int64 } }

    UserStatus: { type: string, enum: [active, disabled] }
    DataScopeType: { type: string, enum: [self, department, organization] }
    AdministrationUserWrite:
      type: object
      required: [externalSubject, email, displayName]
      properties: { externalSubject: { type: string, maxLength: 255 }, email: { type: string, format: email, maxLength: 320 }, displayName: { type: string, maxLength: 255 } }
    AdministrationUser:
      allOf: [{ $ref: '#/components/schemas/AdministrationUserWrite' }, { type: object, required: [id, status, roleGrants, version, createdAt, updatedAt], properties: { id: { type: integer, format: int64 }, status: { $ref: '#/components/schemas/UserStatus' }, roleGrants: { type: array, items: { $ref: '#/components/schemas/RoleGrant' } }, version: { type: integer, format: int64 }, createdAt: { type: string, format: date-time }, updatedAt: { type: string, format: date-time } } }]
    AdministrationUserPage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/AdministrationUser' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }
    RoleDefinition:
      type: object
      required: [code, name, permissions, allowedDataScopes]
      properties: { code: { type: string }, name: { type: string }, description: { type: string, nullable: true }, permissions: { type: array, items: { type: string } }, allowedDataScopes: { type: array, items: { $ref: '#/components/schemas/DataScopeType' } } }
    RoleGrantWrite:
      type: object
      required: [roleCode, dataScopeType, dataScopeId]
      properties: { roleCode: { type: string, maxLength: 80 }, dataScopeType: { $ref: '#/components/schemas/DataScopeType' }, dataScopeId: { type: integer, format: int64, minimum: 0 } }
    RoleGrant:
      allOf: [{ $ref: '#/components/schemas/RoleGrantWrite' }, { type: object, required: [grantedAt], properties: { grantedAt: { type: string, format: date-time }, grantedBy: { type: integer, format: int64, nullable: true } } }]
    AuditResult: { type: string, enum: [succeeded, rejected, failed] }
    AuditLog:
      type: object
      required: [id, action, entityType, entityId, result, correlationId, occurredAt]
      properties: { id: { type: integer, format: int64 }, actorUserId: { type: integer, format: int64, nullable: true }, action: { type: string }, entityType: { type: string }, entityId: { type: string }, beforeData: { type: object, additionalProperties: true, nullable: true, description: Redacted and field-allowlisted. }, afterData: { type: object, additionalProperties: true, nullable: true, description: Redacted and field-allowlisted. }, result: { $ref: '#/components/schemas/AuditResult' }, correlationId: { type: string }, occurredAt: { type: string, format: date-time } }
    AuditLogPage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/AuditLog' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }
    DeliveryStatus: { type: string, enum: [pending, processing, delivered, retry_scheduled, failed, dead_letter] }
    Delivery:
      type: object
      required: [id, messageType, aggregateType, aggregateId, status, occurredAt, availableAt, attempts]
      properties: { id: { type: string, format: uuid }, messageType: { type: string }, aggregateType: { type: string }, aggregateId: { type: string }, status: { $ref: '#/components/schemas/DeliveryStatus' }, occurredAt: { type: string, format: date-time }, availableAt: { type: string, format: date-time }, processedAt: { type: string, format: date-time, nullable: true }, attempts: { type: integer, minimum: 0 }, lastErrorCode: { type: string, nullable: true } }
    DeliveryPage: { type: object, required: [items, page], properties: { items: { type: array, items: { $ref: '#/components/schemas/Delivery' } }, page: { $ref: '#/components/schemas/PageMetadata' } } }
    IntegrationConfigurationWrite:
      type: object
      additionalProperties: false
      required: [enabled, settings]
      properties: { enabled: { type: boolean }, settings: { type: object, additionalProperties: true }, rotatedSecret: { type: string, minLength: 16, writeOnly: true, nullable: true } }
    IntegrationConfiguration:
      type: object
      required: [key, provider, enabled, secretConfigured, settings, version, updatedAt]
      properties: { key: { type: string }, provider: { type: string }, enabled: { type: boolean }, secretConfigured: { type: boolean }, settings: { type: object, additionalProperties: true }, lastSuccessfulAt: { type: string, format: date-time, nullable: true }, version: { type: integer, format: int64 }, updatedAt: { type: string, format: date-time } }
    IntegrationTestResult:
      type: object
      required: [id, integrationKey, status, startedAt]
      properties: { id: { type: string, format: uuid }, integrationKey: { type: string }, status: { type: string, enum: [queued, running, succeeded, failed] }, startedAt: { type: string, format: date-time }, completedAt: { type: string, format: date-time, nullable: true }, resultCode: { type: string, nullable: true } }
    HealthStatus:
      type: object
      additionalProperties: false
      required: [status, timestamp]
      properties: { status: { type: string, enum: [healthy, unhealthy] }, timestamp: { type: string, format: date-time } }
